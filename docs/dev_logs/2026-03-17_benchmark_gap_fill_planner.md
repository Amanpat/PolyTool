# Dev Log: Benchmark Gap-Fill Planner

**Date:** 2026-03-17
**Branch:** phase-1
**Objective:** Build a real gap-fill plan from local data; produce
`config/benchmark_v1_gap_fill.targets.json` or an honest insufficiency report.

---

## Files Changed

| File | Action | Reason |
|------|--------|--------|
| `packages/polymarket/benchmark_gap_fill_planner.py` | Created | Discovery + planning module |
| `tests/test_benchmark_gap_fill_planner.py` | Created | 34 offline tests (injectable DuckDB) |
| `docs/specs/SPEC-benchmark-gap-fill-planner-v1.md` | Created | Spec doc |
| `docs/CURRENT_STATE.md` | Updated | Reflect planner shipped and targets written |
| `config/benchmark_v1_gap_fill.targets.json` | Created | 120 real Silver reconstruction targets |
| `config/benchmark_v1_gap_fill.insufficiency.json` | Created | new_market bucket insufficiency report |

---

## Source Coverage Found

### pmxt_archive
- Files: 5 hourly Parquet snapshots (`polymarket_orderbook_2026-03-15T10–T14.parquet`)
- Distinct condition_ids: 15,514
- Window: 2026-03-15T10:00–T14:59 UTC (captured as EDT 06:00–11:00)
- Token IDs: 2 per condition_id (YES + NO); first JB `clob_token_ids` entry used as YES

### Jon-Becker markets
- Total rows: 408,863 markets
- Condition_ids matching pmxt: 9,249
- Latest created_at in matched set: **2026-02-03** (dataset ~40 days stale as of 2026-03-15)

### Join result: 9,249 matched markets (condition_id join, Python-side)

---

## Targets Generated by Bucket

| Bucket | Shortage | Candidates | Priority-1 Selected | Priority-2 (Overflow) |
|--------|----------|-----------|--------------------|-----------------------|
| politics | 9 | 2,052 | 9 | 21 |
| sports | 11 | 2,049 | 11 | 19 |
| crypto | 10 | 279 | 10 | 20 |
| near_resolution | 9 | 272 | 9 | 21 |
| new_market | 5 | **0** | 0 | 0 |

**Total targets written:** 120 (30 per covered bucket)

### near_resolution candidates
Markets with `end_date` within 48h of 2026-03-15.  Includes:
- Oscar/BAFTA award markets resolving on 2026-03-15
- NCAA March Madness seeding markets
- Various event markets

### new_market insufficiency
Root cause: JB dataset snapshot is ~2026-02-03.  No markets created after that
date appear in JB.  The new_market bucket requires markets created within 48h
of 2026-03-15 → irreducible gap from current local data.

---

## Commands Run

```bash
# Ran during development — probe queries
python -c "import duckdb; ..."   # DuckDB schema checks

# Final production run
python -c "
from packages.polymarket.benchmark_gap_fill_planner import run
import json
from pathlib import Path
gap_report = json.loads(Path('config/benchmark_v1.gap_report.json').read_text())
result = run(
    pmxt_root=r'D:\...\pmxt_archive',
    jon_root=r'D:\...\jon_becker',
    gap_report=gap_report,
    out_path=Path('config/benchmark_v1_gap_fill.targets.json'),
    insufficiency_path=Path('config/benchmark_v1_gap_fill.insufficiency.json'),
)
# any_targets=True, fully_sufficient=False, total=120
"
```

---

## Test Results

```
34 passed in 5.68s
  TestClassifyMarket (15 tests) — pure classification, no I/O
  TestGapFillPlanner (12 tests) — injectable pmxt/JB fns, offline
  TestOutputSchema (7 tests) — schema contract + file writes
  TestSmokeRealData (1 test) — real data roots (auto-skips if absent)
```

---

## Open Questions for Generation Packet

1. **new_market**: Requires either a fresher JB snapshot or live shadow
   recordings of newly-listed markets on 2026-03-15.  Shadow recording of
   3–5 new markets would close this gap.

2. **price_2min population**: All 120 targets have `price_2min_ready=false`.
   Must run `fetch-price-2min --token-id <ID>` for each priority-1 token
   before `batch-reconstruct-silver`.

3. **Silver quality check**: After reconstruction, re-run `benchmark-manifest`
   to confirm Silver tapes classify correctly and fill the shortage buckets.
   Silver tapes with `reconstruction_confidence="low"` may not meet the bar.

4. **Token ordering**: Using first `clob_token_ids` entry from JB as YES token.
   Should validate against actual market data for a sample before full batch.
