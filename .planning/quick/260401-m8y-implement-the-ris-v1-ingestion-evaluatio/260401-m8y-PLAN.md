---
phase: quick
plan: 260401-m8y
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/research/evaluation/__init__.py
  - packages/research/evaluation/hard_stops.py
  - packages/research/evaluation/scoring.py
  - packages/research/evaluation/providers.py
  - packages/research/evaluation/evaluator.py
  - packages/research/evaluation/types.py
  - packages/research/synthesis/__init__.py
  - packages/research/synthesis/precheck.py
  - packages/research/synthesis/precheck_ledger.py
  - tools/cli/research_eval.py
  - tools/cli/research_precheck.py
  - polytool/__main__.py
  - config/research_eval_prompt.md
  - tests/test_ris_evaluation.py
  - tests/test_ris_precheck.py
  - docs/dev_logs/2026-04-01_ris_v1_evaluation_precheck.md
autonomous: true

must_haves:
  truths:
    - "A document can be evaluated offline with no cloud API keys set, returning a scored gate decision"
    - "Hard stops reject documents before scoring when extraction fails, body is empty, language mismatches, or content is spam/malformed"
    - "Source-family-aware credibility guidance is applied during scoring (academic vs social vs github)"
    - "A precheck can be run against a topic/idea and returns GO/CAUTION/STOP with supporting rationale"
    - "Every precheck outcome is appended to a JSONL ledger at artifacts/research/prechecks/precheck_ledger.jsonl"
    - "CLI commands research-eval and research-precheck are registered and functional"
    - "All existing 2787+ tests still pass"
  artifacts:
    - path: "packages/research/evaluation/evaluator.py"
      provides: "Document evaluation pipeline with hard stops + scoring + gate decision"
      exports: ["DocumentEvaluator", "evaluate_document"]
    - path: "packages/research/evaluation/hard_stops.py"
      provides: "Universal hard stop checks before scoring"
      exports: ["check_hard_stops", "HardStopResult"]
    - path: "packages/research/evaluation/scoring.py"
      provides: "4-dimension scoring rubric with source-family guidance"
      exports: ["score_document", "ScoringResult"]
    - path: "packages/research/evaluation/providers.py"
      provides: "LLM provider abstraction - local/manual default, cloud opt-in"
      exports: ["EvalProvider", "ManualProvider", "OllamaProvider"]
    - path: "packages/research/synthesis/precheck.py"
      provides: "Precheck runner: GO/CAUTION/STOP with evidence + contradiction hook"
      exports: ["run_precheck", "PrecheckResult"]
    - path: "packages/research/synthesis/precheck_ledger.py"
      provides: "JSONL append-only ledger for precheck outcomes"
      exports: ["append_precheck", "list_prechecks"]
    - path: "tools/cli/research_eval.py"
      provides: "CLI entrypoint for document evaluation"
    - path: "tools/cli/research_precheck.py"
      provides: "CLI entrypoint for precheck"
    - path: "tests/test_ris_evaluation.py"
      provides: "Offline tests for evaluation gate, hard stops, scoring"
    - path: "tests/test_ris_precheck.py"
      provides: "Offline tests for precheck runner, ledger, CLI"
  key_links:
    - from: "packages/research/evaluation/evaluator.py"
      to: "packages/research/evaluation/hard_stops.py"
      via: "check_hard_stops() called before scoring"
      pattern: "check_hard_stops"
    - from: "packages/research/evaluation/evaluator.py"
      to: "packages/research/evaluation/providers.py"
      via: "provider.score() called for LLM-based scoring"
      pattern: "provider\\.score"
    - from: "packages/research/synthesis/precheck.py"
      to: "packages/research/synthesis/precheck_ledger.py"
      via: "append_precheck() called after every precheck run"
      pattern: "append_precheck"
    - from: "polytool/__main__.py"
      to: "tools/cli/research_eval.py"
      via: "_command_entrypoint registration"
      pattern: "research_eval_main"
---

<objective>
Implement the RIS v1 evaluation gate and precheck workflow as offline-first library modules with CLI integration.

Purpose: Provide PolyTool with a document quality gate and pre-development check system that prevents wasted effort (the "pair accumulation problem"). This is the core reusable evaluation infrastructure that all future RIS source pipelines (academic, social, etc.) will feed through.

Output: Evaluation pipeline (hard stops + 4-dimension scoring + gate decision), precheck runner (GO/CAUTION/STOP with contradiction hook), JSONL precheck ledger, two CLI commands (`research-eval`, `research-precheck`), comprehensive offline tests.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Coding Projects/Polymarket/PolyTool/CLAUDE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/reference/RAGfiles/RIS_OVERVIEW.md
@D:/Coding Projects/Polymarket/PolyTool/docs/reference/RAGfiles/RIS_03_EVALUATION_GATE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/reference/RAGfiles/RIS_04_KNOWLEDGE_STORE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/reference/RAGfiles/RIS_05_SYNTHESIS_ENGINE.md

<interfaces>
<!-- Existing patterns the executor MUST follow. Extracted from codebase. -->

From packages/research/hypotheses/registry.py (JSONL append-only pattern to replicate):
```python
SCHEMA_VERSION = "hypothesis_registry_v0"

def _utcnow() -> datetime:
    return datetime.now(timezone.utc).replace(microsecond=0)

def _iso_utc(value: datetime) -> str:
    return value.astimezone(timezone.utc).replace(microsecond=0).isoformat()

def append_event(path: Path, event: dict) -> None:
    """Append a single JSON event to a JSONL file (atomic, newline-terminated)."""

def _iter_events(path: Path):
    """Yield parsed dicts from a JSONL file, skipping blank/corrupt lines."""

def stable_hypothesis_id(description: str) -> str:
    """SHA256-based deterministic ID from description text."""
```

From packages/polymarket/rag/metadata.py (ID generation pattern):
```python
def compute_doc_id(source: str, raw_text: str, *, max_len: int = 12) -> str:
    """SHA256-based deterministic document ID."""
```

From packages/polymarket/rag/query.py (RAG similarity — for contradiction hook):
```python
def query_index(
    index_dir: str, query_text: str, *, n_results: int = 5,
    doc_type: str | None = None, include_metadata: bool = True
) -> list[dict]:
```

From polytool/__main__.py (CLI registration pattern):
```python
# 1. Create entrypoint variable at module level:
research_eval_main = _command_entrypoint("tools.cli.research_eval")

# 2. Add to _COMMAND_HANDLER_NAMES dict:
"research-eval": "research_eval_main",

# 3. Add usage text in print_usage() under appropriate section

# tools/cli/*.py pattern: main(argv: list[str]) -> int
```

From packages/polymarket/rag/eval.py (dataclass pattern):
```python
@dataclass
class EvalCase:
    query: str
    expected_doc_ids: list[str]
    mode: str = "any"
    tags: list[str] = field(default_factory=list)

@dataclass
class CaseResult:
    query: str
    expected: list[str]
    retrieved: list[str]
    hit: bool
    mode: str
    tags: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core evaluation gate library — types, hard stops, scoring, providers, evaluator</name>
  <files>
    packages/research/evaluation/__init__.py,
    packages/research/evaluation/types.py,
    packages/research/evaluation/hard_stops.py,
    packages/research/evaluation/scoring.py,
    packages/research/evaluation/providers.py,
    packages/research/evaluation/evaluator.py,
    config/research_eval_prompt.md,
    tests/test_ris_evaluation.py
  </files>
  <behavior>
    - HardStopResult with fields: passed (bool), reason (str|None), stop_type (str|None)
    - check_hard_stops rejects empty body (body is None, empty, or whitespace-only) with stop_type="empty_body"
    - check_hard_stops rejects body shorter than 50 chars with stop_type="too_short"
    - check_hard_stops rejects body that is >80% non-ASCII with stop_type="encoding_garbage"
    - check_hard_stops rejects body that matches spam patterns (all-caps >60%, repeated URLs >3) with stop_type="spam_malformed"
    - check_hard_stops passes a normal English paragraph
    - ScoringResult with fields: relevance (1-5), novelty (1-5), actionability (1-5), credibility (1-5), total (int), epistemic_type (str), summary (str), key_findings (list[str]), eval_model (str)
    - EvalProvider ABC with score(document: dict, prompt: str) -> dict method
    - ManualProvider.score() returns a placeholder ScoringResult with all dimensions=3 (total=12), eval_model="manual_placeholder", so pipeline works without any LLM
    - OllamaProvider.score() calls localhost Ollama endpoint (test with mock/monkeypatch)
    - DocumentEvaluator.evaluate(document) runs hard_stops first; if failed, returns gate="REJECT" with hard_stop reason, never calls provider
    - DocumentEvaluator.evaluate(document) with ManualProvider returns gate="ACCEPT" for total>=12, gate="REVIEW" for 8-12, gate="REJECT" for <8
    - Source-family credibility guidance: SOURCE_FAMILY_GUIDANCE dict maps source_type to a string hint injected into scoring prompt
    - EvalDocument dataclass: doc_id, title, author, source_type, source_url, source_publish_date, body, metadata (dict)
    - Gate thresholds: GREEN >= 12, YELLOW 8-12, RED < 8
  </behavior>
  <action>
    Create `packages/research/evaluation/types.py`:
    - `EvalDocument` dataclass: doc_id (str), title (str), author (str), source_type (str), source_url (str), source_publish_date (str|None), body (str), metadata (dict, default_factory=dict).
    - `HardStopResult` dataclass: passed (bool), reason (str|None=None), stop_type (str|None=None).
    - `ScoringResult` dataclass: relevance (int), novelty (int), actionability (int), credibility (int), total (int), epistemic_type (str), summary (str), key_findings (list[str]), eval_model (str). Add a `gate` property that returns "ACCEPT" if total>=12, "REVIEW" if 8<=total<12, "REJECT" if total<8.
    - `GateDecision` dataclass: gate (str: ACCEPT|REVIEW|REJECT), scores (ScoringResult|None), hard_stop (HardStopResult|None), doc_id (str), timestamp (str).
    - `SOURCE_FAMILIES` dict mapping source_type strings to family names: {"arxiv": "academic", "ssrn": "academic", "book": "academic", "reddit": "forum_social", "twitter": "forum_social", "youtube": "forum_social", "github": "github", "blog": "blog", "news": "news", "dossier": "dossier_report", "manual": "manual"}.
    - `SOURCE_FAMILY_GUIDANCE` dict mapping family name to a one-sentence scoring guidance string. For "academic": "Academic/peer-reviewed source. Credibility floor is 3 unless methodology is clearly flawed. Weight empirical findings heavily." For "forum_social": "Community source. Credibility ceiling is 3 unless the author provides verifiable data or on-chain evidence. Look past conversational filler for core insight." For "github": "Open-source practitioner source. Credibility 3-4 if repo has evidence of real usage. Score actionability high if code is directly adaptable." For "blog": "Blog/essay source. Credibility depends on author track record and evidence quality. Score novelty relative to existing knowledge base." For "dossier_report": "Internal analysis report. High relevance by default. Score novelty based on whether findings are already captured." For "manual": "Manually submitted content. Apply standard rubric without source-type bias." For "news": "News article. Credibility depends on outlet reputation. Actionability is usually low unless it contains market-moving data."

    Create `packages/research/evaluation/hard_stops.py`:
    - `check_hard_stops(doc: EvalDocument) -> HardStopResult` function.
    - Check order: (1) empty_body — body is None or stripped length == 0, (2) too_short — stripped body < 50 chars, (3) encoding_garbage — count non-ASCII chars / total chars > 0.80, (4) spam_malformed — uppercase ratio > 0.60 of alpha chars OR repeated URL pattern (same URL appearing 4+ times).
    - Return HardStopResult(passed=True) if all checks pass.
    - Each failure returns HardStopResult(passed=False, reason="...", stop_type="...").

    Create `packages/research/evaluation/scoring.py`:
    - `build_scoring_prompt(doc: EvalDocument) -> str` — constructs the full evaluation prompt. Include the 4-dimension rubric (relevance, novelty, actionability, credibility) with 1-5 scale descriptions from RIS_03. Include `SOURCE_FAMILY_GUIDANCE[family]` for the document's source type. Include epistemic type tagging instruction. Output format: JSON with keys relevance, novelty, actionability, credibility (each {score, rationale}), epistemic_type, summary, key_findings.
    - `parse_scoring_response(raw_json: str, model_name: str) -> ScoringResult` — parses LLM JSON response into ScoringResult. Handles missing fields gracefully (default score=1 for missing dimensions). Computes total. Sets eval_model.
    - `score_document(doc: EvalDocument, provider: "EvalProvider") -> ScoringResult` — calls provider.score(doc, prompt) and parses the result.

    Create `packages/research/evaluation/providers.py`:
    - `EvalProvider` ABC with: `score(self, doc: EvalDocument, prompt: str) -> str` (returns raw JSON string), `name` property (str).
    - `ManualProvider(EvalProvider)` — `score()` returns a hardcoded JSON string with all dimensions=3, epistemic_type="UNKNOWN", summary="Manual placeholder — human review required", key_findings=[], eval_model="manual_placeholder". This is the default provider that makes the pipeline work with zero external dependencies.
    - `OllamaProvider(EvalProvider)` — `__init__(self, model: str = "qwen3:30b", base_url: str = "http://localhost:11434")`. `score()` sends POST to `{base_url}/api/generate` with the prompt and `format: "json"`. Raises `ConnectionError` on network failure (caller handles gracefully). Uses `urllib.request` (no requests dependency needed — stdlib only).
    - `get_provider(name: str = "manual", **kwargs) -> EvalProvider` factory function. Supports "manual" and "ollama". Raises ValueError for unknown provider names. Cloud providers (gemini, deepseek) are NOT implemented — leave a comment: `# Cloud providers (gemini, deepseek) deferred to RIS v2. See RIS_03_EVALUATION_GATE.md`.

    Create `packages/research/evaluation/evaluator.py`:
    - `DocumentEvaluator` class. `__init__(self, provider: EvalProvider | None = None)` — defaults to ManualProvider if None.
    - `evaluate(self, doc: EvalDocument) -> GateDecision` — (1) run check_hard_stops, (2) if failed, return GateDecision(gate="REJECT", scores=None, hard_stop=result, doc_id=doc.doc_id, timestamp=_iso_utc(now)), (3) if passed, call score_document(doc, self.provider), (4) return GateDecision with gate from scores.gate property.
    - `evaluate_document(doc: EvalDocument, provider_name: str = "manual", **kwargs) -> GateDecision` — module-level convenience function that creates DocumentEvaluator with get_provider(provider_name) and calls evaluate.
    - Import `_utcnow` and `_iso_utc` patterns from the hypothesis registry style (redefine locally to avoid cross-package import).

    Create `config/research_eval_prompt.md`:
    - Full evaluation rubric in markdown format matching RIS_03 spec.
    - 4 dimensions with score descriptions.
    - Source family guidance section.
    - 3 example evaluations (1 accept, 1 reject, 1 borderline).
    - Epistemic type tagging instructions.
    - Expected JSON output format.

    Create `packages/research/evaluation/__init__.py`:
    - Docstring: `"""RIS v1 evaluation gate — document quality scoring and hard-stop filtering."""`
    - Export: `DocumentEvaluator`, `evaluate_document`, `EvalDocument`, `GateDecision`, `HardStopResult`, `ScoringResult`, `check_hard_stops`.

    Create `tests/test_ris_evaluation.py`:
    - Write tests FIRST (RED), then verify they fail, then implement (GREEN).
    - Test hard stops: empty body, too_short, encoding_garbage, spam_malformed, valid_passes.
    - Test ScoringResult.gate property: total=15 -> ACCEPT, total=10 -> REVIEW, total=5 -> REJECT.
    - Test ManualProvider returns well-formed JSON, all scores=3, total=12.
    - Test DocumentEvaluator with ManualProvider: hard stop doc returns REJECT without scoring; valid doc returns ACCEPT (since manual gives 12).
    - Test SOURCE_FAMILY_GUIDANCE has entries for all families in SOURCE_FAMILIES.values().
    - Test build_scoring_prompt includes source family guidance text.
    - Test parse_scoring_response handles valid JSON and malformed JSON (graceful defaults).
    - Test evaluate_document convenience function works end-to-end with manual provider.
    - All tests must be fully offline — no network, no LLM, no Chroma.
    - Target: 15-20 focused tests.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -m pytest tests/test_ris_evaluation.py -v --tb=short -x</automated>
  </verify>
  <done>
    - packages/research/evaluation/ contains types.py, hard_stops.py, scoring.py, providers.py, evaluator.py, __init__.py
    - config/research_eval_prompt.md exists with full rubric
    - All hard stop types are tested and working: empty_body, too_short, encoding_garbage, spam_malformed
    - ManualProvider produces gate=ACCEPT (total 12) for any valid document
    - OllamaProvider implemented with urllib.request (no new dependencies)
    - Source-family credibility guidance is applied in scoring prompt
    - 15+ tests pass in test_ris_evaluation.py
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Precheck runner, JSONL ledger, contradiction hook, CLI commands, and integration</name>
  <files>
    packages/research/synthesis/__init__.py,
    packages/research/synthesis/precheck.py,
    packages/research/synthesis/precheck_ledger.py,
    tools/cli/research_eval.py,
    tools/cli/research_precheck.py,
    polytool/__main__.py,
    tests/test_ris_precheck.py,
    docs/dev_logs/2026-04-01_ris_v1_evaluation_precheck.md
  </files>
  <behavior>
    - PrecheckResult with fields: recommendation (GO|CAUTION|STOP), idea (str), supporting_evidence (list[str]), contradicting_evidence (list[str]), risk_factors (list[str]), stale_warning (bool), timestamp (str), provider_used (str)
    - run_precheck(idea, provider_name="manual") returns PrecheckResult; with ManualProvider returns CAUTION as default recommendation
    - Precheck prompt includes contradiction detection instructions: "Identify evidence that supports AND contradicts the idea"
    - Stale-evidence warning: if no documents referenced or all referenced docs have source_publish_date before 2023, set stale_warning=True
    - append_precheck writes a JSONL line to artifacts/research/prechecks/precheck_ledger.jsonl (creates dir + file if not exists)
    - list_prechecks reads back all entries from the ledger file
    - list_prechecks on non-existent file returns empty list (not error)
    - CLI research-precheck --idea "text" runs precheck, prints result, appends to ledger
    - CLI research-eval --file path/to/doc.md evaluates a document, prints gate decision
    - CLI research-eval --title "T" --body "B" --source-type arxiv evaluates inline content
    - Both CLI commands return exit code 0 on success
    - research-eval and research-precheck appear in polytool --help under a new "Research Intelligence" section
  </behavior>
  <action>
    Create `packages/research/synthesis/precheck_ledger.py`:
    - Reuse the JSONL append-only pattern from `packages/research/hypotheses/registry.py`.
    - `LEDGER_SCHEMA_VERSION = "precheck_ledger_v0"`
    - `DEFAULT_LEDGER_PATH = Path("artifacts/research/prechecks/precheck_ledger.jsonl")`
    - `append_precheck(result: "PrecheckResult", ledger_path: Path | None = None) -> None` — serializes PrecheckResult to dict, adds `schema_version` and `event_type: "precheck_run"`, writes one JSON line. Creates parent dirs if missing.
    - `list_prechecks(ledger_path: Path | None = None) -> list[dict]` — reads all lines from ledger, yields parsed dicts, skips blank/corrupt lines. Returns empty list if file does not exist.
    - `_iter_events(path: Path)` — generator that yields parsed dicts from JSONL, matching the hypothesis registry pattern exactly.
    - Redefine `_utcnow()` and `_iso_utc()` locally (same pattern as hypothesis registry — avoid cross-package import coupling).

    Create `packages/research/synthesis/precheck.py`:
    - `PrecheckResult` dataclass: recommendation (str: GO|CAUTION|STOP), idea (str), supporting_evidence (list[str]), contradicting_evidence (list[str]), risk_factors (list[str]), stale_warning (bool, default=False), timestamp (str), provider_used (str), raw_response (str, default="").
    - `build_precheck_prompt(idea: str) -> str` — constructs the precheck prompt. Instructions: evaluate the idea against prediction market domain knowledge, identify supporting evidence, contradicting evidence, risk factors. Contradiction detection is explicit in the prompt: "You MUST identify at least one risk factor even for promising ideas." Output format: JSON with recommendation, supporting_evidence, contradicting_evidence, risk_factors.
    - `parse_precheck_response(raw_json: str, idea: str, model_name: str) -> PrecheckResult` — parses LLM JSON into PrecheckResult. Default recommendation is CAUTION if parsing fails or recommendation is missing. Sets stale_warning=False by default (future: wire to RAG doc dates). Sets timestamp to _iso_utc(_utcnow()).
    - `check_stale_evidence(result: PrecheckResult) -> PrecheckResult` — stub that returns result unchanged but with a TODO comment: `# TODO: Wire to RAG query to check document dates. If all cited sources are pre-2023 or no sources found, set stale_warning=True.` For now, stale_warning stays False.
    - `find_contradictions(idea: str) -> list[str]` — stub that returns empty list with TODO comment: `# TODO: Wire to packages/polymarket/rag/query.py query_index() to find documents that may contradict the idea. Use semantic similarity search, then LLM to classify support vs contradiction.`
    - `run_precheck(idea: str, provider_name: str = "manual", ledger_path: Path | None = None, **kwargs) -> PrecheckResult` — (1) build prompt, (2) get provider via get_provider(provider_name), (3) call provider.score() with a synthetic EvalDocument (doc_id=f"precheck_{stable_id}", body=idea, source_type="manual"), (4) parse response, (5) check_stale_evidence, (6) append to ledger via append_precheck, (7) return result.
    - Note: For ManualProvider, the score() output will not match precheck JSON format. Handle this gracefully: if parsing fails, return PrecheckResult with recommendation="CAUTION", idea=idea, supporting_evidence=["Manual evaluation — no LLM analysis performed"], contradicting_evidence=[], risk_factors=["No automated analysis available — manual review recommended"], stale_warning=False.

    Create `packages/research/synthesis/__init__.py`:
    - Docstring: `"""RIS v1 synthesis — precheck runner and research report generation."""`
    - Export: `run_precheck`, `PrecheckResult`, `append_precheck`, `list_prechecks`.

    Create `tools/cli/research_eval.py`:
    - Standard CLI pattern: `main(argv: list[str]) -> int`
    - argparse with subcommand-free design (flat flags).
    - `--file PATH` — read document from file (markdown or plain text). Title from filename, body from content.
    - `--title TEXT` — document title (required if no --file).
    - `--body TEXT` — document body inline (required if no --file).
    - `--source-type TYPE` — source type (default: "manual"). Choices from SOURCE_FAMILIES.keys().
    - `--author TEXT` — author (default: "unknown").
    - `--provider NAME` — evaluation provider (default: "manual"). Choices: manual, ollama.
    - `--json` — output raw JSON instead of formatted text.
    - Logic: construct EvalDocument, call evaluate_document(), print result. If --file: read file, use stem as title, content as body.
    - Print format (non-JSON): "Gate: ACCEPT/REVIEW/REJECT | Total: N/20 | R:N N:N A:N C:N | Model: provider_name". If hard-stopped: "Gate: REJECT | Hard stop: {stop_type} — {reason}".
    - Return 0 on success, 1 on argument error, 2 on evaluation error.

    Create `tools/cli/research_precheck.py`:
    - Standard CLI pattern: `main(argv: list[str]) -> int`
    - `--idea TEXT` (required) — the idea or concept to precheck.
    - `--provider NAME` — provider (default: "manual").
    - `--ledger PATH` — custom ledger path (default: artifacts/research/prechecks/precheck_ledger.jsonl).
    - `--json` — output raw JSON instead of formatted text.
    - `--no-ledger` — skip writing to ledger (useful for dry-run).
    - Logic: call run_precheck(idea, provider_name, ledger_path if not no_ledger else None), print result.
    - Print format: "Recommendation: GO/CAUTION/STOP\n\nIdea: {idea}\n\nSupporting:\n  - {evidence}\n\nContradicting:\n  - {evidence}\n\nRisks:\n  - {risk}\n\nStale warning: {yes/no}".
    - Return 0 on success, 1 on argument error.

    Update `polytool/__main__.py`:
    - Add two entrypoint variables near the existing ones (after line ~78):
      `research_eval_main = _command_entrypoint("tools.cli.research_eval")`
      `research_precheck_main = _command_entrypoint("tools.cli.research_precheck")`
    - Add to `_COMMAND_HANDLER_NAMES` dict:
      `"research-eval": "research_eval_main",`
      `"research-precheck": "research_precheck_main",`
    - Add a new section in `print_usage()` AFTER the "RAG & Knowledge" section and BEFORE the "Crypto Pair Bot" section:
      ```
      print("")
      print("--- Research Intelligence (RIS v1) --------------------------------------")
      print("  research-eval         Evaluate a document through the RIS quality gate")
      print("  research-precheck     Pre-development check: GO / CAUTION / STOP recommendation")
      ```

    Create `tests/test_ris_precheck.py`:
    - Write tests FIRST (RED), then verify they fail, then implement (GREEN).
    - Test PrecheckResult dataclass fields and defaults.
    - Test build_precheck_prompt includes contradiction detection language.
    - Test parse_precheck_response with valid JSON.
    - Test parse_precheck_response with invalid JSON returns CAUTION default.
    - Test run_precheck with ManualProvider returns CAUTION with manual-mode fallback message.
    - Test append_precheck writes to JSONL file (use tmp_path fixture).
    - Test list_prechecks reads back appended entries.
    - Test list_prechecks on non-existent path returns empty list.
    - Test append_precheck creates parent directories.
    - Test run_precheck with ledger_path=None skips ledger append (no file created).
    - Test CLI research_precheck.main(["--idea", "test idea"]) returns 0 (use tmp_path for ledger via monkeypatch).
    - Test CLI research_eval.main(["--title", "T", "--body", "Valid body text here...", "--source-type", "manual"]) returns 0.
    - Test CLI research_eval.main(["--file", "path/to/test.md"]) reads file and returns 0 (create temp file).
    - All tests must be fully offline — no network, no LLM, no Chroma.
    - Target: 15-20 focused tests.

    Create `docs/dev_logs/2026-04-01_ris_v1_evaluation_precheck.md`:
    - Standard dev log format.
    - Document: what was built, architectural decisions (offline-first, ManualProvider default, JSONL ledger pattern reuse, source-family guidance approach, contradiction hook as stub), files created, test counts, known limitations (cloud providers deferred, contradiction detection is stub, stale-evidence check is stub, no Chroma integration yet), next steps (wire Ollama for real scoring, connect RAG for contradiction detection, build academic/social ingestion pipelines per RIS_01/RIS_02).

    Run the full test suite to confirm no regressions:
    `python -m pytest tests/ -x -q --tb=short`
    Confirm 2787+ existing tests still pass plus the new RIS tests.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -m pytest tests/test_ris_precheck.py tests/test_ris_evaluation.py -v --tb=short -x && python -m polytool research-eval --title "Test" --body "This is a sufficiently long test body for evaluation gate testing purposes, containing enough text to pass hard stops." --source-type manual && python -m polytool research-precheck --idea "Test precheck idea" --no-ledger && python -m polytool --help</automated>
  </verify>
  <done>
    - packages/research/synthesis/ contains precheck.py, precheck_ledger.py, __init__.py
    - JSONL ledger pattern works: append_precheck writes, list_prechecks reads
    - run_precheck returns PrecheckResult with GO/CAUTION/STOP
    - Contradiction hook interface exists (stub with TODO)
    - Stale-evidence warning interface exists (stub with TODO)
    - CLI research-eval evaluates documents and prints gate decision
    - CLI research-precheck runs precheck, prints recommendation, appends to ledger
    - Both commands appear in polytool --help under "Research Intelligence (RIS v1)" section
    - 15+ tests pass in test_ris_precheck.py
    - Full test suite (2787+ existing tests + ~30 new RIS tests) passes with zero regressions
    - Dev log written at docs/dev_logs/2026-04-01_ris_v1_evaluation_precheck.md
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_ris_evaluation.py tests/test_ris_precheck.py -v --tb=short` — all RIS tests pass
2. `python -m pytest tests/ -x -q --tb=short` — full suite passes with zero regressions (2787+ existing + ~30 new)
3. `python -m polytool --help` — shows "Research Intelligence (RIS v1)" section with research-eval and research-precheck
4. `python -m polytool research-eval --title "Test Doc" --body "A sufficiently long body..." --source-type arxiv` — prints gate decision
5. `python -m polytool research-precheck --idea "Is crypto pair accumulation viable?" --no-ledger` — prints GO/CAUTION/STOP
6. `python -m polytool research-precheck --idea "Test ledger write"` — creates artifacts/research/prechecks/precheck_ledger.jsonl with one entry
</verification>

<success_criteria>
- Evaluation gate pipeline works end-to-end offline with ManualProvider (no cloud API keys needed)
- Hard stops reject bad documents before scoring (empty, too short, encoding garbage, spam)
- Source-family credibility guidance is injected into scoring prompts based on source_type
- Precheck returns GO/CAUTION/STOP with supporting/contradicting evidence fields
- Every precheck run is logged to JSONL ledger (unless --no-ledger)
- Contradiction hook interface is defined (stub implementation, ready for RAG wiring)
- Stale-evidence warning interface is defined (stub implementation)
- Two CLI commands registered and functional: research-eval, research-precheck
- All existing tests pass without regression
- Dev log documents decisions and known limitations
</success_criteria>

<output>
After completion, create `.planning/quick/260401-m8y-implement-the-ris-v1-ingestion-evaluatio/260401-m8y-SUMMARY.md`
</output>
