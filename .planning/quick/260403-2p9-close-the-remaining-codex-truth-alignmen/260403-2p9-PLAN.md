---
phase: quick-260403-2p9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md
  - tests/test_ris_ops_metrics.py
  - docs/features/FEATURE-ris-scheduler-v1.md
  - docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md
autonomous: true
requirements: [RIS_06_truth_alignment, RIS_06_cli_coverage]
must_haves:
  truths:
    - "RIS_06 spec CLI examples match the actual shipped command names"
    - "research-stats CLI has dedicated test coverage for summary, export, --json, and error paths"
    - "APScheduler optional-install behavior is documented truthfully in the spec"
  artifacts:
    - path: "docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md"
      provides: "Corrected CLI command examples matching shipped reality"
      contains: "research-scheduler status"
    - path: "tests/test_ris_ops_metrics.py"
      provides: "CLI-level tests for research-stats command"
      min_lines: 350
    - path: "docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md"
      provides: "Dev log covering all changes, commands run, test results"
  key_links:
    - from: "tests/test_ris_ops_metrics.py"
      to: "tools/cli/research_stats.py"
      via: "from tools.cli.research_stats import main"
      pattern: "research_stats.*main"
    - from: "docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md"
      to: "polytool/__main__.py"
      via: "CLI command names must match registered names"
      pattern: "research-scheduler|research-stats|research-health"
---

<objective>
Close the remaining truth-alignment and CLI-coverage gaps in RIS_06 infrastructure docs and tests.

Purpose: The RIS_06 spec was written before implementation and contains CLI command names that
do not match the shipped commands. The research-stats CLI shipped with module-level tests only
and zero CLI-level tests. This plan fixes the doc-to-code drift, adds missing CLI test coverage,
and documents APScheduler's optional install behavior truthfully.

Output: Updated RIS_06 spec with correct CLI examples, expanded test file with CLI tests for
research-stats, and a dev log documenting all changes.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md
@tools/cli/research_stats.py
@tools/cli/research_scheduler.py
@tests/test_ris_ops_metrics.py
@tests/test_ris_scheduler.py
@docs/features/FEATURE-ris-scheduler-v1.md
@docs/features/FEATURE-ris-ops-cli-and-metrics.md
@polytool/__main__.py (lines 89-90, 150-151, 211-212 for registration)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tools/cli/research_stats.py:
```python
def main(argv: list) -> int:
    """CLI entrypoint for research-stats command. Returns int exit code."""
# Subcommands: "summary" (--json), "export" (--out PATH)
# Common path-override args: --db, --eval-artifacts-dir, --precheck-ledger, --report-dir, --acquisition-review-dir
# Missing subcommand -> parser.print_help() -> return 1
```

From tools/cli/research_scheduler.py:
```python
def main(argv: Optional[list] = None) -> int:
    """CLI entrypoint. Returns int exit code."""
# Subcommands: "status" (--json), "start" (--dry-run), "run-job JOB_ID" (--json)
```

From packages/research/metrics.py:
```python
from packages.research.metrics import (
    RisMetricsSnapshot,      # dataclass with to_dict()
    collect_ris_metrics,     # -> RisMetricsSnapshot (accepts path overrides)
    format_metrics_summary,  # RisMetricsSnapshot -> str
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RIS_06 spec CLI truth alignment and APScheduler optional docs</name>
  <files>docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md, docs/features/FEATURE-ris-scheduler-v1.md</files>
  <action>
Update docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md with the following targeted fixes:

1. **Line 205** (`polytool research stats [--days N]`): Replace with:
   ```
   # Show operator metrics snapshot
   python -m polytool research-stats summary [--json]
   python -m polytool research-stats export [--out PATH]
   ```
   Add a parenthetical note: "(Shipped as standalone `research-stats` command, not `research stats` subcommand.)"

2. **Lines 236-237** (`polytool research scheduler-status`): Replace with:
   ```
   # Show scheduler status (list registered jobs)
   python -m polytool research-scheduler status
   python -m polytool research-scheduler status --json
   python -m polytool research-scheduler start --dry-run
   ```
   Add a parenthetical note: "(Shipped as standalone `research-scheduler` command with subcommands, not `research scheduler-status`.)"

3. **APScheduler optional install note**: In the section near lines 18-27 (the APScheduler scheduling section), add a paragraph after the code block:
   ```
   **Note (shipped behavior):** APScheduler is an optional dependency in the `[ris]` extras group
   (`pip install 'polytool[ris]'`). The `JOB_REGISTRY` and all job metadata are importable
   WITHOUT APScheduler installed. APScheduler is only required when `start_research_scheduler()`
   is called to actually start the background loop. The `research-scheduler status` and
   `research-scheduler run-job` subcommands work without APScheduler.
   ```

Do NOT rewrite unrelated sections. Only the three targeted edits above. Keep all other content unchanged.

Verify that docs/features/FEATURE-ris-scheduler-v1.md already has the correct CLI examples (it does -- `python -m polytool research-scheduler status` etc. on lines 41-55). No changes needed to that file -- just confirm.
  </action>
  <verify>
    <automated>grep -n "research-scheduler status" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md && grep -n "research-stats summary" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md && grep -c "scheduler-status" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md</automated>
  </verify>
  <done>
    - RIS_06 spec line 205 area shows `research-stats summary` and `research-stats export` (not `research stats`)
    - RIS_06 spec line 237 area shows `research-scheduler status` (not `research scheduler-status`)
    - APScheduler optional install note present in the scheduling section
    - Zero occurrences of the old `scheduler-status` command name remain (grep count = 0)
    - Feature docs remain unchanged (already correct)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI-level tests for research-stats command</name>
  <files>tests/test_ris_ops_metrics.py</files>
  <behavior>
    - test_cli_summary_returns_0: main(["summary"]) with tmp_path overrides returns 0 and prints output containing "Knowledge Store"
    - test_cli_summary_json_returns_valid_json: main(["summary", "--json"]) with tmp_path overrides returns 0, stdout is valid JSON with "generated_at" and "total_docs" keys
    - test_cli_export_writes_file: main(["export", "--out", str(tmp_path/"out.json")]) with tmp_path overrides returns 0 and creates the file; file is valid JSON
    - test_cli_missing_subcommand_returns_1: main([]) returns 1
    - test_cli_export_creates_parent_dirs: main(["export", "--out", str(tmp_path/"sub"/"dir"/"out.json")]) with tmp_path overrides returns 0 and nested path exists
    - test_cli_summary_with_populated_data: With a pre-populated KS db and eval artifacts, main(["summary", "--json"]) returns JSON with total_docs > 0 and gate_distribution populated
  </behavior>
  <action>
Append a new test class `TestResearchStatsCLI` to the bottom of tests/test_ris_ops_metrics.py.

The key challenge: `research_stats.main()` calls `collect_ris_metrics()` which reads from default
paths. Tests must override these paths to use tmp_path. There are two approaches:
- Use the CLI `--db`, `--eval-artifacts-dir`, `--precheck-ledger`, `--report-dir`, `--acquisition-review-dir` flags (preferred -- tests the real CLI path)
- For `export`, also pass `--out` to write to tmp_path

Each test should:
1. Create a minimal KS sqlite db via `_create_ks_db()` (helper already exists in the test file)
2. Pass all path override flags pointing to tmp_path subdirs
3. Capture stdout via `io.StringIO()` and `contextlib.redirect_stdout`
4. Assert exit code and output content

Import at top of new class: `from tools.cli.research_stats import main as stats_main`

Use the existing `_create_ks_db` helper to create test databases. For tests that need eval
artifacts, use the existing `_write_eval_artifacts` helper.

Add `import io` and `from contextlib import redirect_stdout` to the imports at the top of the
file if not already present.

Do NOT modify existing tests. Only append new tests at the bottom.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ris_ops_metrics.py -x -q --tb=short</automated>
  </verify>
  <done>
    - tests/test_ris_ops_metrics.py has >= 21 tests (15 existing + 6 new CLI tests)
    - All tests pass with 0 failures
    - New tests cover: summary, summary --json, export, missing subcommand, nested export path, populated data with --json
    - No network calls in any test
    - Existing 15 tests still pass unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Write dev log and verify no regressions</name>
  <files>docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md</files>
  <action>
Create docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md with the standard dev log format:

```markdown
# Dev Log: RIS R4 -- CLI Truth Alignment and Coverage Gaps

**Date:** 2026-04-03
**Task:** quick-260403-2p9
**Objective:** Close RIS_06 doc-to-code truth drift and add missing CLI test coverage.

---

## Files Modified

| File | Role | Action |
|---|---|---|
| docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md | RIS_06 spec | Updated CLI examples to match shipped commands |
| tests/test_ris_ops_metrics.py | CLI + metrics tests | Added 6 CLI-level tests for research-stats |
| docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md | This dev log | Created |

---

## Gaps Fixed

### 1. Doc truth: `scheduler-status` -> `research-scheduler status`
RIS_06 spec said `polytool research scheduler-status`. Shipped CLI is `python -m polytool research-scheduler status`.
Updated spec to match shipped reality.

### 2. Doc truth: `research stats` -> `research-stats summary`
RIS_06 spec said `polytool research stats [--days N]`. Shipped CLI is `python -m polytool research-stats summary [--json]`.
Updated spec to match shipped reality.

### 3. APScheduler optional install behavior
Added note to RIS_06 spec documenting that APScheduler is optional (`pip install 'polytool[ris]'`), JOB_REGISTRY imports without it, and `status`/`run-job` subcommands work without it.

### 4. research-stats CLI test coverage
Added 6 new tests in TestResearchStatsCLI: summary exit code, JSON output, export file creation, missing subcommand, nested directory creation, populated data JSON.

## Gaps Explicitly Deferred

- ClickHouse write path for RIS metrics (RIS_06 v2)
- APScheduler integration tests with real scheduler (requires APScheduler installed; existing injectable tests are sufficient)
- research-health CLI test expansion (already has 6 tests in test_ris_monitoring.py)

---

## Commands Run

[Fill in after execution with actual test counts and results]

---

## Codex Review Tier

**Skip** -- docs and tests only, no execution/trading code.
```

Then run the full regression suite:
```
python -m pytest tests/test_ris_ops_metrics.py tests/test_ris_scheduler.py tests/test_ris_monitoring.py -v --tb=short
```

Update the dev log with actual test counts from the run.

Finally, run the quick smoke test:
```
python -m polytool --help
```
Verify `research-stats` and `research-scheduler` and `research-health` all appear in the help output.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ris_ops_metrics.py tests/test_ris_scheduler.py -x -q --tb=short</automated>
  </verify>
  <done>
    - Dev log exists at docs/dev_logs/2026-04-03_ris_r4_cli_truth_alignment.md
    - Dev log lists all files modified, gaps fixed, gaps deferred, and test results
    - All RIS-related tests pass (test_ris_ops_metrics + test_ris_scheduler + test_ris_monitoring)
    - `python -m polytool --help` shows research-stats, research-scheduler, research-health
  </done>
</task>

</tasks>

<verification>
1. `grep -c "scheduler-status" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md` returns 0 (old command name gone)
2. `grep -c "research-scheduler status" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md` returns >= 2 (correct command)
3. `grep -c "research-stats summary" docs/reference/RAGfiles/RIS_06_INFRASTRUCTURE.md` returns >= 1 (correct command)
4. `python -m pytest tests/test_ris_ops_metrics.py -q --tb=short` shows >= 21 passed, 0 failed
5. `python -m pytest tests/test_ris_scheduler.py -q --tb=short` shows 28 passed, 0 failed
6. `python -m polytool --help | grep -c "research-"` returns >= 3
</verification>

<success_criteria>
- RIS_06 spec CLI examples match shipped reality (zero stale command names)
- research-stats CLI has >= 6 dedicated test cases covering all subcommands and error paths
- APScheduler optional-install behavior documented in the spec
- All existing tests still pass (no regressions)
- Dev log documents changes, test results, and deferred items
</success_criteria>

<output>
After completion, create `.planning/quick/260403-2p9-close-the-remaining-codex-truth-alignmen/260403-2p9-SUMMARY.md`
</output>
