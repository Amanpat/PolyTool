---
phase: quick-260402-wj3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/research/ingestion/fetchers.py
  - packages/research/ingestion/adapters.py
  - packages/research/ingestion/normalize.py
  - tools/cli/research_ingest.py
  - tools/cli/research_acquire.py
  - tests/test_ris_academic_ingest_v1.py
  - tests/fixtures/ris_external_sources/book_sample.json
  - docs/features/FEATURE-ris-academic-ingest-v1.md
  - docs/dev_logs/2026-04-02_ris_r1_academic_ingestion_completion.md
  - docs/CURRENT_STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ArXiv papers can be fetched by topic keyword via research-acquire --search"
    - "Books can be ingested with stable book_id/chapter/section identity via --source-family book"
    - "research-ingest and research-acquire both expose --extract-claims flag"
    - "SSRN status is documented truthfully as deferred (no live scraping)"
    - "All new paths have deterministic fixture-driven offline tests"
  artifacts:
    - path: "packages/research/ingestion/fetchers.py"
      provides: "LiveAcademicFetcher.search_by_topic() method"
    - path: "packages/research/ingestion/adapters.py"
      provides: "BookAdapter class + ADAPTER_REGISTRY['book'] entry"
    - path: "tools/cli/research_acquire.py"
      provides: "--search and --extract-claims flags"
    - path: "tools/cli/research_ingest.py"
      provides: "--extract-claims flag"
    - path: "tests/test_ris_academic_ingest_v1.py"
      provides: "Offline tests for all new paths"
    - path: "docs/features/FEATURE-ris-academic-ingest-v1.md"
      provides: "Truthful RIS_01 v1 status with SSRN deferred"
  key_links:
    - from: "tools/cli/research_acquire.py"
      to: "packages/research/ingestion/fetchers.LiveAcademicFetcher.search_by_topic"
      via: "--search flag triggers topic search loop"
    - from: "tools/cli/research_acquire.py"
      to: "packages/research/ingestion/pipeline.IngestPipeline.ingest_external"
      via: "post_ingest_extract=True when --extract-claims passed"
    - from: "packages/research/ingestion/adapters.BookAdapter"
      to: "ADAPTER_REGISTRY"
      via: "registered as 'book' family key"
---

<objective>
Complete the practical v1 scope of RIS_01 Academic Ingestion Pipeline by closing four concrete gaps:
(1) ArXiv topic search, (2) curated book ingestion with stable metadata identity, (3) opt-in
claim extraction flag on both CLIs, and (4) truthful SSRN deferred documentation.

Purpose: RIS has all building blocks (adapter, fetcher, cache, pipeline, CLI) but lacks ArXiv
topic search, a first-class book ingestion path, and the --extract-claims flag that connects
ingest to claim extraction. This closes those gaps and documents SSRN status honestly.

Output: Extended fetchers.py, new BookAdapter in adapters.py, --search/--extract-claims CLI
flags, a book_sample.json fixture, a comprehensive offline test suite, and updated docs.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Coding Projects/Polymarket/PolyTool/CLAUDE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/CURRENT_STATE.md

# Key prior-phase files the executor needs
@D:/Coding Projects/Polymarket/PolyTool/packages/research/ingestion/fetchers.py
@D:/Coding Projects/Polymarket/PolyTool/packages/research/ingestion/adapters.py
@D:/Coding Projects/Polymarket/PolyTool/packages/research/ingestion/pipeline.py
@D:/Coding Projects/Polymarket/PolyTool/tools/cli/research_ingest.py
@D:/Coding Projects/Polymarket/PolyTool/tools/cli/research_acquire.py

<interfaces>
<!-- Existing contracts the executor must preserve and extend. -->

From packages/research/ingestion/fetchers.py:
- LiveAcademicFetcher(timeout=15, _http_fn=None).fetch(url) -> dict
  Returns: {url, title, abstract, authors (list[str]), published_date}
  Raises: FetchError if URL is not arXiv or on network error
  arXiv Atom API: http://export.arxiv.org/api/query?id_list={arxiv_id}&max_results=1
  arXiv search API: http://export.arxiv.org/api/query?search_query=...&max_results=N
  _ATOM_NS = {"atom": "http://www.w3.org/2005/Atom"}

From packages/research/ingestion/adapters.py:
- ADAPTER_REGISTRY: dict[str, type[SourceAdapter]] = {"academic", "github", "blog", "news"}
- AcademicAdapter.adapt(raw_source, cache=None) -> ExtractedDocument
  raw_source keys: url, title, abstract, authors, published_date, body_text (optional)
  source_type inference: "arxiv" (arxiv.org URL), "ssrn" (ssrn.com URL), "book" (default)
- BookAdapter must handle: title, authors, book_id, chapter, section, body_text
  source_family="book", source_type="book"
  stable ID: sha256(book_id + chapter or section) for canonical_url

From packages/research/ingestion/pipeline.py:
- IngestPipeline.ingest(source, *, post_ingest_extract=False, **kwargs) -> IngestResult
  post_ingest_extract=True triggers extract_and_link(store, doc_id) non-fatally
- IngestPipeline.ingest_external(raw_source, source_family, *, cache=None, **kwargs) -> IngestResult
  Does NOT currently accept post_ingest_extract. Must add it.
- IngestResult: doc_id, chunk_count, gate_decision, rejected, reject_reason

FETCHER_REGISTRY = {"academic": LiveAcademicFetcher, "github": ..., "blog": ..., "news": ...}
ADAPTER_REGISTRY = {"academic": AcademicAdapter, "github": ..., "blog": ..., "news": ...}

Note: "book" is NOT in FETCHER_REGISTRY (no live book fetcher — books are local files).
"book" MUST be in ADAPTER_REGISTRY (BookAdapter for structured book metadata).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ArXiv topic search + BookAdapter + ingest_external claim extraction</name>
  <files>
    packages/research/ingestion/fetchers.py,
    packages/research/ingestion/adapters.py,
    packages/research/ingestion/normalize.py,
    packages/research/ingestion/pipeline.py,
    tests/fixtures/ris_external_sources/book_sample.json,
    tests/test_ris_academic_ingest_v1.py
  </files>
  <behavior>
    ArXiv topic search:
    - LiveAcademicFetcher.search_by_topic(query, max_results=5) returns list[dict]
    - Each dict has same keys as fetch(): url, title, abstract, authors, published_date
    - Uses arXiv Atom API: http://export.arxiv.org/api/query?search_query={encoded_query}&max_results={n}
    - Parses multiple atom:entry elements (same parsing as fetch())
    - Returns [] when API returns zero entries (not raises)
    - FetchError on network failure (consistent with fetch())
    - _http_fn injectable for offline testing (same pattern as existing fetch())
    - Test: search_by_topic with mock XML returning 2 entries returns list of 2 dicts
    - Test: search_by_topic with empty Atom feed returns []
    - Test: search_by_topic with network error raises FetchError

    BookAdapter:
    - raw_source keys: title (str), authors (str or list[str]), book_id (str),
      chapter (str, optional), section (str, optional), body_text (str),
      published_date (str, optional)
    - source_family = "book", source_type = "book"
    - canonical_url = "internal://book/{book_id}/{chapter_or_section_slug}"
      where chapter_or_section_slug = slugify(chapter or section or "root")
      slugify: lowercase, replace spaces with _, strip non-alnum except _
    - stable book_id stored in canonical_ids as {"book_id": book_id}
    - body = body_text (required; hard-stop will catch empty bodies)
    - Registered in ADAPTER_REGISTRY as "book"
    - Test: BookAdapter.adapt with full keys returns ExtractedDocument with source_family="book"
    - Test: canonical_url uses book_id + chapter slug
    - Test: missing chapter falls back to "root" in canonical_url
    - Test: authors as list -> comma-joined author string; str -> passthrough

    book_sample.json fixture:
    - {"title": "Market Microstructure Theory", "authors": ["Maureen O'Hara"],
       "book_id": "market_microstructure_theory", "chapter": "Chapter 3: The Inventory Model",
       "body_text": "The inventory model is central to understanding...", "published_date": "1995-01-01"}

    ingest_external claim extraction:
    - IngestPipeline.ingest_external() must accept post_ingest_extract=False kwarg
    - When post_ingest_extract=True, call extract_and_link(store, doc_id) non-fatally
      (same pattern as ingest())
    - Test: ingest_external with post_ingest_extract=True calls extract_and_link (mock confirms call)
    - Test: ingest_external with post_ingest_extract=False does NOT call extract_and_link
    - Test: extract_and_link exception does not fail ingest (result.rejected stays False)
  </behavior>
  <action>
    STEP 1: Write tests/test_ris_academic_ingest_v1.py with all behavior cases above (RED).
    All tests use injectable _http_fn / mocks — no network calls.
    Create tests/fixtures/ris_external_sources/book_sample.json.

    STEP 2: Implement the following changes (GREEN):

    fetchers.py — add search_by_topic():
    - Add method to LiveAcademicFetcher after fetch():
      def search_by_topic(self, query: str, max_results: int = 5) -> list[dict]:
    - URL: import urllib.parse; encoded = urllib.parse.quote_plus(query)
      api_url = f"http://export.arxiv.org/api/query?search_query=all:{encoded}&max_results={max_results}"
    - Parse same Atom XML, iterate all atom:entry elements with same field extraction as fetch()
    - Return list of dicts, each with same keys as fetch() return
    - Return [] if no entries found (no FetchError)

    adapters.py — add BookAdapter:
    - Add BookAdapter(SourceAdapter) class after AcademicAdapter
    - Implement slugify helper: re.sub(r'[^\w]', '_', text.lower()).strip('_')
    - canonical_url = f"internal://book/{book_id}/{slug(chapter or section or 'root')}"
    - Call self._cache_if_provided() with source_family="book"
    - Register in ADAPTER_REGISTRY: add "book": BookAdapter

    normalize.py — if _infer_source_type() is used by BookAdapter, ensure "book" passes through
    cleanly. Check that internal:// URLs do not crash canonicalize_url(). If canonicalize_url
    raises on internal:// scheme, add guard: if not url.startswith("http"):  return url.
    No other normalize changes needed.

    pipeline.py — extend ingest_external():
    - Add post_ingest_extract: bool = False as explicit kwarg in the method signature
      (pull it from **kwargs before passing anything else)
    - After doc_id is set, add the same non-fatal extract_and_link block that ingest() uses:
      if post_ingest_extract and doc_id:
          try:
              from packages.research.ingestion.claim_extractor import extract_and_link
              extract_and_link(self._store, doc_id)
          except Exception:
              pass

    Run tests after each file change. All new tests must pass.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ris_academic_ingest_v1.py -v --tb=short</automated>
  </verify>
  <done>
    - All test_ris_academic_ingest_v1.py tests pass
    - LiveAcademicFetcher.search_by_topic() returns list[dict] with injectable _http_fn
    - BookAdapter registered in ADAPTER_REGISTRY["book"]
    - ingest_external() accepts post_ingest_extract kwarg
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI --search and --extract-claims flags + docs + dev log</name>
  <files>
    tools/cli/research_acquire.py,
    tools/cli/research_ingest.py,
    docs/features/FEATURE-ris-academic-ingest-v1.md,
    docs/dev_logs/2026-04-02_ris_r1_academic_ingestion_completion.md,
    docs/CURRENT_STATE.md
  </files>
  <action>
    tools/cli/research_acquire.py — three changes:

    (A) --search flag for ArXiv topic search:
    - Add --search QUERY argument (mutually exclusive with existing --url if needed, or
      treat as alternative mode: if --search is given, --url is not required)
    - Add --max-results N argument (default 5, only used with --search)
    - Implementation: call fetcher.search_by_topic(query, max_results), iterate results,
      call pipeline.ingest_external() for each paper, collect IngestResult list
    - Output in JSON mode: list of per-paper result dicts; human mode: one line per paper
    - --source-family defaults to "academic" when --search is used (can be overridden)
    - If --search is given and fetcher does not have search_by_topic (non-academic family),
      print error and return 1
    - NOTE: --search uses LiveAcademicFetcher._http_fn for live HTTP — it is opt-in and
      intended for operator use; no automated tests hit the network

    (B) --extract-claims flag:
    - Add --extract-claims / dest="extract_claims" store_true argument
    - When set, pass post_ingest_extract=True to pipeline.ingest_external()
    - Works in both --url and --search modes

    (C) --source-family: add "book" to choices list (for --from-adapter path in research_ingest.py)

    tools/cli/research_ingest.py — one change:
    - Add --extract-claims / dest="extract_claims" store_true argument
    - When set, pass post_ingest_extract=True to pipeline.ingest() (already supported)
    - Add "book" to --source-family choices list

    docs/features/FEATURE-ris-academic-ingest-v1.md — new feature doc:
    Write a complete feature doc covering:
    - Status: Shipped (2026-04-02) — RIS_01 practical v1
    - What shipped: ArXiv by-ID fetch (Phase 5), ArXiv topic search (this task),
      BookAdapter, --extract-claims, --search CLI
    - SSRN status (truthful): "SSRN source_type is detected by URL pattern when a URL
      containing 'ssrn.com' is submitted via --from-adapter or --url. There is no live
      SSRN fetcher. Live SSRN scraping is DEFERRED — SSRN blocks automated crawlers and
      requires session-authenticated access. Operators can ingest SSRN papers manually
      by supplying pre-fetched raw_source dicts via --from-adapter."
    - Manual local file ingest: already works via research-ingest --file (Phase 1)
    - Manual URL submission: already works via research-acquire --url (Phase 5)
    - Curated book ingest: new BookAdapter, use research-ingest --from-adapter with
      --source-family book, or research-acquire --from-adapter (book has no live fetcher)
    - Claim extraction: --extract-claims flag on both CLIs
    - What remains deferred: SSRN live scraper, semantic chunking by section, Chroma wiring

    docs/dev_logs/2026-04-02_ris_r1_academic_ingestion_completion.md — mandatory dev log:
    - Objective
    - Files changed and why (table)
    - Commands run + output (use placeholder output where live commands would require network)
    - Test results (exact counts)
    - What parts of RIS_01 are now complete
    - What remains explicitly deferred (SSRN live, Chroma wiring, semantic chunking)
    - Codex review tier: Skip

    docs/CURRENT_STATE.md — add a brief bullet under recent progress:
    - "quick-260402-wj3: RIS_01 practical v1 closure — ArXiv topic search, BookAdapter,
      --extract-claims / --search CLI flags; SSRN truthfully deferred; N new tests"

    Verification after CLI changes: run smoke path using the existing book fixture:
    python -m polytool research-ingest \
      --from-adapter tests/fixtures/ris_external_sources/book_sample.json \
      --source-family book --no-eval --json

    Run full regression to confirm no regressions:
    python -m pytest tests/ -x -q --tb=short
  </action>
  <verify>
    <automated>python -m pytest tests/test_ris_academic_ingest_v1.py tests/test_ris_ingestion_integration.py tests/test_ris_phase4_source_acquisition.py tests/test_ris_fetchers.py -q --tb=short</automated>
  </verify>
  <done>
    - research-ingest and research-acquire both accept --extract-claims
    - research-acquire accepts --search QUERY --max-results N for ArXiv topic search
    - --source-family book is accepted by research-ingest --from-adapter
    - FEATURE-ris-academic-ingest-v1.md documents SSRN as deferred (not implied as working)
    - Dev log written with exact test results
    - Full regression: same or better pass count vs baseline (3349 passing)
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Core functionality smoke tests (offline):
   python -m pytest tests/test_ris_academic_ingest_v1.py -v --tb=short

2. Regression suite:
   python -m pytest tests/ -x -q --tb=short
   Expected: >= 3349 tests passing, 0 new failures

3. CLI smoke (book fixture must exist from Task 1):
   python -m polytool research-ingest \
     --from-adapter tests/fixtures/ris_external_sources/book_sample.json \
     --source-family book --no-eval --json
   Expected: JSON output with doc_id, rejected=false

4. CLI --extract-claims help text visible:
   python -m polytool research-ingest --help | grep extract-claims
   python -m polytool research-acquire --help | grep extract-claims
</verification>

<success_criteria>
- LiveAcademicFetcher.search_by_topic() method exists and passes all offline tests
- BookAdapter in ADAPTER_REGISTRY["book"] with stable book_id/chapter canonical_url
- IngestPipeline.ingest_external() accepts post_ingest_extract kwarg
- Both CLIs expose --extract-claims flag
- research-acquire exposes --search QUERY for ArXiv topic search
- SSRN status documented as deferred (not implied as working live)
- All new tests are offline/deterministic (no network calls in default pytest run)
- No regression in existing test suite (>= 3349 passing)
- Dev log written with exact results
- CURRENT_STATE.md updated
</success_criteria>

<output>
After completion, create .planning/quick/260402-wj3-complete-the-practical-v1-scope-of-ris-0/260402-wj3-SUMMARY.md
</output>
