---
phase: quick-260408-oz0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/polymarket/rag/eval.py
  - tools/cli/rag_eval.py
  - docs/eval/sample_queries.jsonl
  - docs/eval/ris_retrieval_benchmark.jsonl
  - tests/test_rag_eval.py
  - docs/dev_logs/2026-04-08_ris_phase2_retrieval_benchmark_truth.md
autonomous: true
requirements: ["Phase2-Item7"]

must_haves:
  truths:
    - "Eval report shows per-query-class (factual, analytical, exploratory) metrics for each retrieval mode"
    - "Eval report includes all required metrics: query_count, mean_recall_at_k, mean_mrr_at_k, total_scope_violations, queries_with_violations, mean_latency_ms, p50_latency_ms, p95_latency_ms"
    - "Baseline artifact (report.json) includes corpus_hash, config metadata, and timestamp"
    - "Same eval suite + same config reproduces structurally identical report"
    - "CLI prints both overall and per-class summary table"
  artifacts:
    - path: "packages/polymarket/rag/eval.py"
      provides: "Per-query-class aggregation in EvalCase, run_eval, write_report"
      contains: "query_class"
    - path: "docs/eval/ris_retrieval_benchmark.jsonl"
      provides: "Phase 2 benchmark suite with query_class annotations"
      contains: "query_class"
    - path: "tools/cli/rag_eval.py"
      provides: "CLI with per-class summary output"
      contains: "query_class"
    - path: "tests/test_rag_eval.py"
      provides: "Tests for per-class segmentation and baseline artifact metadata"
      contains: "query_class"
  key_links:
    - from: "tools/cli/rag_eval.py"
      to: "packages/polymarket/rag/eval.py"
      via: "run_eval + write_report"
      pattern: "run_eval|write_report"
    - from: "packages/polymarket/rag/eval.py"
      to: "docs/eval/ris_retrieval_benchmark.jsonl"
      via: "load_suite parses query_class from JSONL"
      pattern: "query_class"
---

<objective>
Extend the existing RAG eval harness to produce Phase 2 segmented retrieval benchmark
metrics by query class, save reproducible baseline artifacts with corpus hash and config
metadata, and give the operator a clear per-class report of current RAG retrieval quality.

Purpose: Phase 2 item 7 requires segmented benchmark metrics so the operator can see
retrieval quality broken down by query class (factual, analytical, exploratory) and make
informed decisions about RAG tuning. Currently the eval harness only reports overall
aggregates per mode with no class segmentation and no reproducibility metadata.

Output: Extended eval.py with per-class aggregation, updated CLI with per-class display,
Phase 2 benchmark suite JSONL, tests, and dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/specs/SPEC-ris-phase2-operational-contracts.md (Retrieval benchmark reporting contract)
@docs/roadmaps/RIS_PHASE2_evaluation_gate_monitoring_rag_testing_v1_1.md (Item 7)
@packages/polymarket/rag/eval.py (existing eval harness - the main file to extend)
@tools/cli/rag_eval.py (existing CLI - extend for per-class output)
@tests/test_rag_eval.py (existing tests - add per-class and metadata tests)
@docs/eval/sample_queries.jsonl (existing eval suite - reference format)

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/polymarket/rag/eval.py (CURRENT — to be extended):
```python
@dataclass
class EvalCase:
    query: str
    filters: dict
    must_include_any: list[str]
    must_exclude_any: list[str]
    notes: str = ""
    label: str = ""
    # NOTE: no query_class field yet — must add

@dataclass
class CaseResult:
    query: str
    label: str
    mode: str  # "vector" | "lexical" | "hybrid" | "hybrid+rerank"
    recall_at_k: float
    mrr_at_k: float
    scope_violations: list[dict]
    latency_ms: float
    result_count: int
    notes: str = ""
    # NOTE: no query_class field yet — must add

@dataclass
class ModeAggregate:
    mean_recall_at_k: float
    mean_mrr_at_k: float
    total_scope_violations: int
    queries_with_violations: int
    mean_latency_ms: float
    case_results: list[CaseResult]
    # NOTE: missing query_count, p50_latency_ms, p95_latency_ms

@dataclass
class EvalReport:
    timestamp: str
    suite_path: str
    k: int
    modes: dict[str, ModeAggregate]
    # NOTE: missing per_class aggregation, corpus_hash, config metadata

def load_suite(path: Path) -> list[EvalCase]: ...
def run_eval(suite, *, k=8, embedder=None, ..., suite_path="") -> EvalReport: ...
def write_report(report: EvalReport, output_dir: Path) -> tuple[Path, Path]: ...
def _percentile(values: list[float], pct: float) -> float: ...  # Already exists
```

Required Phase 2 metrics per the operational contracts spec:
```
query_count, mean_recall_at_k, mean_mrr_at_k, total_scope_violations,
queries_with_violations, mean_latency_ms, p50_latency_ms, p95_latency_ms
```

Required query classes: factual, analytical, exploratory

Existing JSONL suite format (extend with query_class):
```json
{"query": "...", "query_class": "factual", "filters": {...}, "expect": {...}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend eval harness with query_class segmentation and baseline metadata</name>
  <files>
    packages/polymarket/rag/eval.py
    tests/test_rag_eval.py
  </files>
  <behavior>
    - Test: EvalCase with query_class field loads correctly from JSONL with "query_class" key
    - Test: EvalCase without query_class defaults to "unclassified"
    - Test: CaseResult carries query_class from its EvalCase
    - Test: ModeAggregate includes query_count, p50_latency_ms, p95_latency_ms
    - Test: EvalReport includes per_class_modes dict mapping query_class -> {mode_name -> ModeAggregate}
    - Test: EvalReport includes corpus_hash (SHA-256 of suite file bytes) and eval_config dict
    - Test: write_report JSON artifact contains per_class_modes, corpus_hash, eval_config
    - Test: write_report markdown summary includes per-query-class table section
    - Test: _aggregate_cases helper correctly segments CaseResults by query_class
    - Test: Empty query_class group produces zero-valued aggregates
  </behavior>
  <action>
Extend the RAG eval harness in packages/polymarket/rag/eval.py:

1. Add `query_class: str = "unclassified"` field to `EvalCase` dataclass.

2. Add `query_class: str = "unclassified"` field to `CaseResult` dataclass.

3. Add `query_count: int`, `p50_latency_ms: float`, `p95_latency_ms: float` fields to `ModeAggregate` dataclass. These are additional required metrics per the Phase 2 operational contracts.

4. Extend `EvalReport` with:
   - `per_class_modes: dict[str, dict[str, ModeAggregate]]` — maps query_class -> mode_name -> ModeAggregate
   - `corpus_hash: str` — SHA-256 hex digest of the suite file bytes for reproducibility
   - `eval_config: dict` — captures k, top_k_vector, top_k_lexical, rrf_k, rerank_top_n, embedder model name, reranker model name (or null), suite_path

5. Update `load_suite()` to parse `query_class` from each JSONL line's top-level field, defaulting to `"unclassified"` if absent. This preserves backward compatibility with existing suites.

6. Update `run_eval()`:
   - Accept and pass through config params needed for `eval_config` (embedder model name, reranker model name).
   - After building mode_results, also build per_class_modes: group CaseResults by query_class, compute ModeAggregate for each class.
   - Populate query_count, p50_latency_ms, p95_latency_ms in all ModeAggregate instances (both overall and per-class).
   - Compute corpus_hash from suite_path if the file exists (hashlib.sha256), else empty string.
   - Build eval_config dict from parameters.
   - Return the enriched EvalReport.

7. Extract a helper `_build_aggregate(case_list: list[CaseResult]) -> ModeAggregate` that computes all metrics including query_count, p50, p95 using the existing `_percentile()` function. Use this for both overall and per-class aggregation to avoid duplication.

8. Update `write_report()`:
   - JSON: include `per_class_modes`, `corpus_hash`, `eval_config` in report.json.
   - Markdown: After the overall per-mode table, add a "## Per-Query-Class Results" section with a table for each query_class showing mode-level metrics (Recall@K, MRR@K, violations, latencies).

9. Add `query_class` field propagation: in `run_eval()`, when creating CaseResult instances, copy `case.query_class` to the CaseResult.

Update tests/test_rag_eval.py:
- Add test class `QueryClassSegmentationTests` that creates a suite with mixed query_class values (factual, analytical, exploratory) and verifies:
  - EvalReport.per_class_modes contains all three classes
  - Each class's ModeAggregate has correct query_count
  - p50_latency_ms and p95_latency_ms are populated (>= 0)
  - corpus_hash is a 64-char hex string
  - eval_config contains expected keys

- Add test `test_load_suite_with_query_class` that loads a JSONL with query_class field and verifies EvalCase.query_class is set correctly.

- Add test `test_load_suite_without_query_class_defaults` that loads a JSONL without query_class and verifies default "unclassified".

- Add test `test_write_report_per_class_in_json` that writes a report and checks the JSON contains per_class_modes, corpus_hash, eval_config.

- Add test `test_write_report_per_class_in_markdown` that writes a report and checks the markdown contains "Per-Query-Class" section.

IMPORTANT: Do NOT change the signature of existing public functions in a breaking way. Add new optional parameters only. Existing tests must continue to pass unchanged.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -m pytest tests/test_rag_eval.py -x -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - EvalCase has query_class field, load_suite parses it, defaults to "unclassified"
    - CaseResult carries query_class
    - ModeAggregate has query_count, p50_latency_ms, p95_latency_ms
    - EvalReport has per_class_modes, corpus_hash, eval_config
    - write_report produces JSON with per_class_modes + metadata and markdown with per-class table
    - All existing tests pass, new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 2 benchmark suite, extend CLI output, write dev log</name>
  <files>
    docs/eval/ris_retrieval_benchmark.jsonl
    docs/eval/sample_queries.jsonl
    tools/cli/rag_eval.py
    docs/dev_logs/2026-04-08_ris_phase2_retrieval_benchmark_truth.md
  </files>
  <action>
1. Create `docs/eval/ris_retrieval_benchmark.jsonl` — a Phase 2 retrieval benchmark suite with query_class annotations. Include at minimum 9 cases (3 per class):

   **factual** (3 cases): Direct lookup queries with specific expected content.
   - "What is the ClickHouse analytics storage schema?" (query_class: factual)
   - "What fee model does PolyTool use for PnL computation?" (query_class: factual)
   - "What are the SimTrader tape tier definitions?" (query_class: factual)

   **analytical** (3 cases): Queries requiring synthesis across multiple documents.
   - "How does the market maker strategy handle inventory risk?" (query_class: analytical)
   - "What are the tradeoffs between Gold and Silver tape tiers for backtesting?" (query_class: analytical)
   - "How do validation gates interact with capital stage progression?" (query_class: analytical)

   **exploratory** (3 cases): Open-ended research queries.
   - "What strategies exist for prediction market profitability?" (query_class: exploratory)
   - "How can research findings improve trading decisions?" (query_class: exploratory)
   - "What approaches exist for market selection scoring?" (query_class: exploratory)

   Each case must have: query, query_class, filters (use sensible defaults like private_only: false), expect (must_include_any and must_exclude_any — use reasonable file path patterns based on known repo docs), notes.

   Also update `docs/eval/sample_queries.jsonl` to add `query_class` annotations to existing 5 cases. Classify them as: first 2 = "factual", third = "exploratory", fourth = "factual", fifth = "factual". This ensures backward compatibility is tested with the existing suite.

2. Extend `tools/cli/rag_eval.py`:
   - After the existing overall mode summary table, add a per-query-class summary section.
   - Print a header "Per-Query-Class Breakdown:" then for each query class, print a mini table like the overall one but only for that class.
   - Include query_count in the output.
   - Access report.per_class_modes (new field from Task 1).
   - Print corpus_hash and eval_config summary at the end of output.
   - Add `--suite-hash-only` flag that loads the suite, computes and prints the SHA-256 hash, then exits. This allows operators to verify corpus identity without running the full eval.

3. Create `docs/dev_logs/2026-04-08_ris_phase2_retrieval_benchmark_truth.md`:
   - Document what was built: per-class segmented metrics in eval harness
   - Document the query classes: factual, analytical, exploratory
   - Document the required metrics now tracked
   - Document the baseline artifact structure (corpus_hash, eval_config, per_class_modes)
   - Document how to run: `python -m polytool rag-eval --suite docs/eval/ris_retrieval_benchmark.jsonl`
   - Document how to verify reproducibility: same suite hash + same config = same structural baseline
   - Note: existing sample_queries.jsonl was annotated with query_class for backward compatibility
   - Reference Phase 2 spec: SPEC-ris-phase2-operational-contracts.md, item 7
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -c "import json; lines = open('docs/eval/ris_retrieval_benchmark.jsonl').readlines(); cases = [json.loads(l) for l in lines if l.strip()]; classes = set(c['query_class'] for c in cases); assert classes == {'factual', 'analytical', 'exploratory'}, f'Expected 3 classes, got {classes}'; assert len(cases) >= 9, f'Expected >= 9 cases, got {len(cases)}'; print(f'OK: {len(cases)} cases across {len(classes)} classes')"</automated>
  </verify>
  <done>
    - ris_retrieval_benchmark.jsonl exists with >= 9 cases across 3 query classes
    - sample_queries.jsonl updated with query_class annotations
    - CLI prints per-class breakdown after overall summary
    - CLI has --suite-hash-only flag
    - Dev log created at docs/dev_logs/2026-04-08_ris_phase2_retrieval_benchmark_truth.md
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL suite file -> eval harness | Suite files are local operator-authored; no untrusted input |
| Eval report artifact -> operator | Reports are read-only artifacts for operator consumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-oz0-01 | T (Tampering) | JSONL suite file | accept | Suite files are local, operator-authored, gitignored artifacts. Corpus hash in report provides tamper evidence. |
| T-oz0-02 | I (Information Disclosure) | eval report.json | accept | Reports contain only query text, metrics, and file paths. No secrets, credentials, or PII. |
| T-oz0-03 | D (Denial of Service) | Large suite file hash | accept | SHA-256 of suite file is bounded by file size; operator-controlled input. No external network calls. |
</threat_model>

<verification>
1. Run all existing RAG eval tests: `python -m pytest tests/test_rag_eval.py -x -v`
2. Verify new JSONL suite has correct structure and all 3 query classes
3. Verify report.json from a test run contains per_class_modes, corpus_hash, eval_config
4. Verify summary.md from a test run contains per-query-class table
5. Run smoke test: `python -m polytool --help` still loads
</verification>

<success_criteria>
- packages/polymarket/rag/eval.py produces per-query-class ModeAggregate for each mode
- All 8 required metrics (query_count, mean_recall_at_k, mean_mrr_at_k, total_scope_violations, queries_with_violations, mean_latency_ms, p50_latency_ms, p95_latency_ms) present in every ModeAggregate
- report.json includes per_class_modes, corpus_hash, eval_config
- summary.md includes per-query-class breakdown section
- ris_retrieval_benchmark.jsonl has >= 9 cases with factual/analytical/exploratory classes
- CLI prints per-class summary after overall summary
- All tests pass (existing + new)
- Dev log created
</success_criteria>

<output>
After completion, create `.planning/quick/260408-oz0-finish-phase-2-retrieval-benchmark-truth/260408-oz0-SUMMARY.md`
</output>
