---
phase: 260414-rr3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/specs/SPEC-phase1b-gold-capture-campaign.md
  - docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md
  - docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md
autonomous: true
requirements:
  - rr3-truth-sync

must_haves:
  truths:
    - "CLAUDE.md Silver tier description warns that Silver is NOT suitable for Gate 2 (no L2 book data, fills will be zero)"
    - "SPEC-phase1b-gold-capture-campaign.md Section 4 and Section 5 reference artifacts/tapes/shadow not artifacts/simtrader/tapes"
    - "CORPUS_GOLD_CAPTURE_RUNBOOK.md Sections 3, 4, and 5 reference artifacts/tapes/shadow not artifacts/simtrader/tapes"
    - "A dev log exists at docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md documenting all changes"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Silver tier description with Gate 2 warning"
      contains: "NOT suitable for Gate 2 sweep"
    - path: "docs/specs/SPEC-phase1b-gold-capture-campaign.md"
      provides: "Corrected corpus_audit path in campaign loop"
      contains: "artifacts/tapes/shadow"
    - path: "docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md"
      provides: "Corrected shadow tape paths in sections 3, 4, and 5"
      contains: "artifacts/tapes/shadow"
    - path: "docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md"
      provides: "Change record for this truth-sync pass"
  key_links:
    - from: "CORPUS_GOLD_CAPTURE_RUNBOOK.md Section 4"
      to: "artifacts/tapes/shadow/<run_id>"
      via: "--tape-dir flag in shadow capture command"
      pattern: "artifacts/tapes/shadow"
    - from: "SPEC-phase1b-gold-capture-campaign.md Section 4"
      to: "artifacts/tapes/shadow"
      via: "corpus_audit.py --tape-roots argument"
      pattern: "artifacts/tapes/shadow"
---

<objective>
Surgically correct four categories of stale truth found by the quick-260414-rep audit:

1. CLAUDE.md Silver tier description implies Silver is adequate for Gate 2 fill-based evaluation —
   contradicted by confirmed zero-fill root cause (gate2_fill_diagnosis 2026-04-14).

2. SPEC-phase1b-gold-capture-campaign.md Sections 4 and 5 reference
   artifacts/simtrader/tapes as the shadow write path — stale since
   gold_capture_hardening (quick-260414-qre) moved shadow tapes to artifacts/tapes/shadow.

3. CORPUS_GOLD_CAPTURE_RUNBOOK.md Sections 3, 4, and 5 reference the same stale path.

4. Mandatory dev log documenting every change made in this pass.

Purpose: Prevent future capture sessions from using the wrong path (silently sending
tapes to a directory invisible to corpus_audit) and prevent future sessions from
treating Silver as Gate 2 viable.

Output: Updated CLAUDE.md, SPEC, RUNBOOK, and new dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Coding Projects/Polymarket/PolyTool/CLAUDE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/specs/SPEC-phase1b-gold-capture-campaign.md
@D:/Coding Projects/Polymarket/PolyTool/docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md
@D:/Coding Projects/Polymarket/PolyTool/docs/dev_logs/2026-04-14_gate2_status_audit_post_capture.md
@D:/Coding Projects/Polymarket/PolyTool/docs/dev_logs/2026-04-14_gold_capture_hardening.md

## Key facts for this docs-only pass

### Stale path (old, wrong)
artifacts/simtrader/tapes

### Canonical path (new, correct — from gold_capture_hardening dev log)
artifacts/tapes/shadow

Shadow tapes now auto-route to artifacts/tapes/shadow when --tape-dir is omitted.
The tape-dir argument in the runbook examples should be updated to show the canonical path.

### Silver tier confirmed limitation (from gate2_fill_diagnosis + status audit)
Silver tapes contain only price_2min_guide events. L2Book never initializes.
Fill engine returns book_not_initialized on every tick. Zero fills.
Silver is useful for autoresearch and price history, but NOT for Gate 2 sweep.

### CLAUDE.md current stale line (line 232)
BEFORE: "- **Silver**: reconstructed tapes from pmxt + Jon-Becker + polymarket-apis, good for Gate 2 and autoresearch."
AFTER:  "- **Silver**: reconstructed tapes from pmxt + Jon-Becker + polymarket-apis, useful for autoresearch and price history; NOT suitable for Gate 2 sweep (no L2 book data — fills will be zero)."

### SPEC stale references
Section 4 (Campaign Loop) — corpus_audit.py call with --tape-roots artifacts/simtrader/tapes
Section 5 (Resumability) — prose: "Each shadow session writes to a new timestamped tape directory under artifacts/simtrader/tapes/"

### RUNBOOK stale references
Section 3 — corpus_audit.py call with --tape-roots artifacts/simtrader/tapes
Section 4 — shadow capture --tape-dir example: artifacts/simtrader/tapes/<BUCKET>_<SLUG>_<YYYYMMDDTHHMMSSZ>
             and specific example: artifacts/simtrader/tapes/crypto_will-btc-...
Section 5 — post-capture corpus_audit.py call with --tape-roots artifacts/simtrader/tapes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CLAUDE.md Silver tier description and SPEC/RUNBOOK stale paths</name>
  <files>CLAUDE.md, docs/specs/SPEC-phase1b-gold-capture-campaign.md, docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md</files>
  <action>
Make these exact surgical edits. Read each file first, then apply changes.

**CLAUDE.md — one change at Tape Tiers section (line ~232):**

FIND (exact):
```
- **Silver**: reconstructed tapes from pmxt + Jon-Becker + polymarket-apis, good for Gate 2 and autoresearch.
```

REPLACE WITH:
```
- **Silver**: reconstructed tapes from pmxt + Jon-Becker + polymarket-apis, useful for autoresearch and price history; NOT suitable for Gate 2 sweep (no L2 book data — fills will be zero).
```

Rationale: gate2_fill_diagnosis (2026-04-14) confirmed Silver tapes contain only price_2min_guide events; L2Book never initializes; fill engine returns book_not_initialized on every tick. "Good for Gate 2" is factually wrong.

---

**SPEC-phase1b-gold-capture-campaign.md — three changes:**

Change 1 — Section 4, corpus_audit.py --tape-roots argument:

FIND (exact block in Step 3 of Section 4):
```
   python tools/gates/corpus_audit.py \
       --tape-roots artifacts/simtrader/tapes \
       --tape-roots artifacts/silver \
       --tape-roots artifacts/tapes \
       --out-dir artifacts/corpus_audit \
       --manifest-out config/recovery_corpus_v1.tape_manifest
```

REPLACE WITH:
```
   python tools/gates/corpus_audit.py \
       --tape-roots artifacts/tapes/shadow \
       --tape-roots artifacts/silver \
       --tape-roots artifacts/tapes \
       --out-dir artifacts/corpus_audit \
       --manifest-out config/recovery_corpus_v1.tape_manifest
```

Change 2 — Section 5 (Resumability Rules), first paragraph opening sentence:

FIND (exact):
```
Each shadow session writes to a new timestamped tape directory under
`artifacts/simtrader/tapes/`. No prior capture session is overwritten.
```

REPLACE WITH:
```
Each shadow session writes to a new timestamped tape directory under
`artifacts/tapes/shadow/`. No prior capture session is overwritten.
```

Do NOT change anything else in this section or anywhere else in the SPEC.

---

**CORPUS_GOLD_CAPTURE_RUNBOOK.md — five changes:**

Change 1 — Section 3 corpus_audit.py call, first --tape-roots:

FIND (exact block in Section 3):
```
python tools/gates/corpus_audit.py \
    --tape-roots artifacts/simtrader/tapes \
    --tape-roots artifacts/silver \
    --tape-roots artifacts/tapes \
    --out-dir artifacts/corpus_audit \
    --manifest-out config/recovery_corpus_v1.tape_manifest
```

REPLACE WITH:
```
python tools/gates/corpus_audit.py \
    --tape-roots artifacts/tapes/shadow \
    --tape-roots artifacts/silver \
    --tape-roots artifacts/tapes \
    --out-dir artifacts/corpus_audit \
    --manifest-out config/recovery_corpus_v1.tape_manifest
```

Change 2 — Section 4 shadow capture command, --tape-dir argument template:

FIND (exact):
```
    --tape-dir artifacts/simtrader/tapes/<BUCKET>_<SLUG>_<YYYYMMDDTHHMMSSZ>
```

REPLACE WITH:
```
    --tape-dir artifacts/tapes/shadow/<BUCKET>_<SLUG>_<YYYYMMDDTHHMMSSZ>
```

Change 3 — Section 4 Required arguments bullet for --tape-dir:

FIND (exact):
```
- `--tape-dir ...`: Timestamped dir under `artifacts/simtrader/tapes/`
```

REPLACE WITH:
```
- `--tape-dir ...`: Timestamped dir under `artifacts/tapes/shadow/` (omit to use auto-routed default)
```

Change 4 — Section 4 Example command, --tape-dir value:

FIND (exact):
```
    --tape-dir "artifacts/simtrader/tapes/crypto_will-btc-be-above-100k_20260326T210000Z"
```

REPLACE WITH:
```
    --tape-dir "artifacts/tapes/shadow/crypto_will-btc-be-above-100k_20260326T210000Z"
```

Change 5 — Section 5 post-capture corpus_audit.py call, first --tape-roots:

FIND (exact block in Section 5):
```
python tools/gates/corpus_audit.py \
    --tape-roots artifacts/simtrader/tapes \
    --tape-roots artifacts/silver \
    --tape-roots artifacts/tapes \
    --out-dir artifacts/corpus_audit \
    --manifest-out config/recovery_corpus_v1.tape_manifest
```

REPLACE WITH:
```
python tools/gates/corpus_audit.py \
    --tape-roots artifacts/tapes/shadow \
    --tape-roots artifacts/silver \
    --tape-roots artifacts/tapes \
    --out-dir artifacts/corpus_audit \
    --manifest-out config/recovery_corpus_v1.tape_manifest
```

Do NOT change anything else in RUNBOOK (version header, section numbering, runbook body text, Gate 2 threshold language, no-live-capital language).
  </action>
  <verify>
    <automated>
      grep "NOT suitable for Gate 2" CLAUDE.md &&
      grep "artifacts/tapes/shadow" docs/specs/SPEC-phase1b-gold-capture-campaign.md &&
      grep -c "artifacts/tapes/shadow" docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md | grep -E "^[4-9]|^[1-9][0-9]" &&
      ! grep "artifacts/simtrader/tapes" docs/specs/SPEC-phase1b-gold-capture-campaign.md &&
      ! grep "artifacts/simtrader/tapes" docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md &&
      ! grep "good for Gate 2" CLAUDE.md
    </automated>
  </verify>
  <done>
    - CLAUDE.md Silver tier no longer says "good for Gate 2"; now warns "NOT suitable for Gate 2 sweep (no L2 book data — fills will be zero)"
    - SPEC Section 4 corpus_audit call uses artifacts/tapes/shadow (not artifacts/simtrader/tapes)
    - SPEC Section 5 prose says artifacts/tapes/shadow (not artifacts/simtrader/tapes)
    - RUNBOOK Sections 3, 4, and 5 all use artifacts/tapes/shadow
    - No occurrence of artifacts/simtrader/tapes remains in SPEC or RUNBOOK
    - No occurrence of "good for Gate 2" remains in CLAUDE.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Write dev log for this truth-sync pass</name>
  <files>docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md</files>
  <action>
Create the dev log at docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md.

The dev log must include:

- Date, task ID (quick-260414-rr3), status (COMPLETE)
- Summary: docs-only truth-sync pass correcting 4 staleness categories found by quick-260414-rep
- Files changed table: CLAUDE.md, SPEC-phase1b-gold-capture-campaign.md, CORPUS_GOLD_CAPTURE_RUNBOOK.md
- Per-file change descriptions with the exact stale text removed and correct text added
- Verification commands run and their output (from Task 1 verify step)
- Any remaining doc ambiguities that are intentionally left for post-re-sweep resolution:
  * SPEC Section 1 still says "Gate 2 is in NOT_RUN state" — this is historical context for the campaign, not a live status; CURRENT_STATE.md is the live status source. This is not a staleness bug in the SPEC (campaign context is accurate for what the SPEC governs).
  * ADR-benchmark-versioning-and-crypto-unavailability.md has a stale escalation deadline note — the ADR is a historical decision record; annotating it is an operator decision, not a mechanical doc sync.
- Codex review tier: Skip (docs only, no execution logic)

Keep the dev log concise — a factual change record, not a narrative essay.
  </action>
  <verify>
    <automated>test -f docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md && grep "quick-260414-rr3" docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md</automated>
  </verify>
  <done>
    - dev log exists at docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md
    - contains task ID quick-260414-rr3
    - lists all three modified files
    - documents remaining known ambiguities left for operator/post-sweep resolution
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator | Operator reads docs and acts on them; stale paths cause silent capture failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-rr3-01 | Tampering | corpus_audit path in SPEC/RUNBOOK | mitigate | This task: replace stale path with canonical artifacts/tapes/shadow |
| T-rr3-02 | Information Disclosure | Silver tier description implying Gate 2 viability | mitigate | This task: explicit "NOT suitable for Gate 2 sweep" warning in CLAUDE.md |
</threat_model>

<verification>
After both tasks complete:

1. grep "NOT suitable for Gate 2" CLAUDE.md
2. grep "artifacts/tapes/shadow" docs/specs/SPEC-phase1b-gold-capture-campaign.md
3. grep "artifacts/tapes/shadow" docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md
4. test -f docs/dev_logs/2026-04-14_gate2_docs_truth_sync.md
5. python -m polytool --help (CLI still loads — no code was changed, but confirm no import break)
</verification>

<success_criteria>
- CLAUDE.md Silver tier explicitly warns "NOT suitable for Gate 2 sweep (no L2 book data — fills will be zero)"
- SPEC-phase1b-gold-capture-campaign.md Sections 4 and 5 reference artifacts/tapes/shadow
- CORPUS_GOLD_CAPTURE_RUNBOOK.md Sections 3, 4, and 5 reference artifacts/tapes/shadow
- Zero occurrences of artifacts/simtrader/tapes in SPEC or RUNBOOK
- Dev log documents all changes with exact before/after text
- No code, tests, manifests, or benchmark files were modified
</success_criteria>

<output>
After completion, create `.planning/quick/260414-rr3-truth-sync-the-stale-gate-2-operator-doc/260414-rr3-SUMMARY.md`
</output>
