---
phase: quick-260406-nb7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CURRENT_STATE.md
  - docs/adr/0013-ris-n8n-pilot-scoped.md
  - docs/RIS_OPERATOR_GUIDE.md
  - docs/RIS_AUDIT_REPORT.md
  - docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "All four touched docs agree that n8n instance-level MCP HTTP backend works on community edition >= 2.14.2 (not Enterprise-only)"
    - "No doc contains the stale phrases 'enterprise-only', 'informational placeholder', or 'not available in community edition' in reference to n8n MCP"
    - "RIS_AUDIT_REPORT.md CLI table lists only the real subcommands: start, status, run-job (not stop or list)"
    - "RIS_OPERATOR_GUIDE.md step 1 no longer says N8N_MCP_BEARER_TOKEN is 'not operative'"
    - "ADR 0013 image-and-versioning section reflects the corrected MCP finding from quick-260406-le7"
    - "CURRENT_STATE.md n8n 2.x migration section reflects the corrected MCP finding"
  artifacts:
    - path: "docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md"
      provides: "Dev log documenting the contradictions found and resolved"
  key_links:
    - from: "docs/adr/0013-ris-n8n-pilot-scoped.md"
      to: "docs/RIS_OPERATOR_GUIDE.md"
      via: "Consistent MCP availability statement"
      pattern: "community edition"
    - from: "docs/CURRENT_STATE.md"
      to: "docs/adr/0013-ris-n8n-pilot-scoped.md"
      via: "Consistent MCP availability statement"
      pattern: "community edition"
---

<objective>
Eliminate the last RIS n8n documentation contradictions blocking Phase N4 sign-off.

Purpose: Four operator-facing docs (CURRENT_STATE.md, ADR 0013, RIS_OPERATOR_GUIDE.md,
RIS_AUDIT_REPORT.md) still contain stale claims that were disproven by quick-260406-le7
(the MCP connection debug session). The le7 session proved via POST probes that the n8n
instance-level MCP HTTP backend works on community edition 2.14.2 -- it is NOT Enterprise-
only. Additionally, the RIS_AUDIT_REPORT CLI table still claims `research-scheduler` has
`stop` and `list` subcommands, which do not exist. The RIS_OPERATOR_GUIDE contradicts
itself internally: step 1 says the bearer token is "not operative" while the later MCP
section correctly says it IS operative.

Output: Four corrected docs + one dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md
@docs/dev_logs/2026-04-06_n8n_2x_instance_mcp_upgrade.md

<interfaces>
<!-- The authoritative truth about n8n MCP, established by quick-260406-le7 probe results: -->

1. n8n instance-level MCP HTTP backend at /mcp-server/http WORKS on community edition
   n8n >= 2.14.2. It is NOT Enterprise-only.

2. Earlier probes (quick-260406-ido) used GET requests without the required Accept header,
   which hit the SPA frontend router and returned 200 HTML. The correct probe is POST with
   Content-Type: application/json and Accept: application/json, text/event-stream plus a
   valid JWT. That returns a correct MCP initialize response:
   {"serverInfo":{"name":"n8n MCP Server","version":"1.1.0"}}

3. N8N_MCP_BEARER_TOKEN is the compose-side env var that n8n reads at container startup.
   It IS operative (not "informational"). The token is a static HS256-signed JWT with
   iss=n8n, aud=mcp-server-api, generated from the n8n UI (Settings -> Instance-level MCP).

4. The real research-scheduler subcommands are: status, start, run-job.
   There is NO stop subcommand and NO list subcommand.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MCP and CLI contradictions in all four docs</name>
  <files>
    docs/CURRENT_STATE.md,
    docs/adr/0013-ris-n8n-pilot-scoped.md,
    docs/RIS_OPERATOR_GUIDE.md,
    docs/RIS_AUDIT_REPORT.md
  </files>
  <action>
Apply the following targeted edits. The authoritative truth source is the probe results
from quick-260406-le7 (docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md).

**1. docs/CURRENT_STATE.md** -- Lines 1559-1575 (n8n 2.x Migration section)

Replace lines 1563-1574 with corrected text:

- Line 1563-1564: Change "signals the direction toward HTTP-accessible MCP tooling
  (Enterprise tier for backend endpoint)" to "provides instance-level MCP at
  `/mcp-server/http` (works on community edition; requires JWT bearer token from
  n8n Settings UI)".

- Lines 1572-1574: Replace the three-line block:
  ```
  - MCP backend endpoint: Enterprise feature only; not available in community edition.
    `/mcp-server/http` path is routed to the SPA frontend (200 HTML). `N8N_MCP_BEARER_TOKEN`
    kept in compose as informational placeholder.
  ```
  with:
  ```
  - MCP backend endpoint: works on community edition n8n >= 2.14.2 (not Enterprise-only).
    `POST /mcp-server/http` with `Accept: application/json, text/event-stream` and valid
    JWT bearer token returns MCP initialize response. `GET` without Accept header hits the
    SPA frontend (200 HTML) -- this caused the earlier false "Enterprise-only" finding.
    `N8N_MCP_BEARER_TOKEN` is the compose-side env var read by n8n at container startup.
    See `docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md` for full probe evidence.
  ```

Also add a new section after the 2.x migration section for the le7 debug session:
  ```
  ## n8n Instance MCP Connection Debug (quick-260406-le7, 2026-04-06)

  - Root cause: Claude Code does NOT expand `${VAR}` in HTTP-type `.mcp.json` entries.
    The literal template strings were sent as-is, causing connection failure.
  - Key finding: n8n instance-level MCP HTTP backend works on community edition 2.14.2
    (not Enterprise-only). Earlier GET probes hit the SPA frontend; correct POST with
    Accept header reaches the real backend.
  - `.mcp.json`: removed the n8n-instance-mcp entry; correct fix is `claude mcp add
    --transport http -s local` with the real token.
  - `.env.example`: real JWT token replaced with placeholder.
  - Multiple docs corrected to remove "Enterprise only" claims.
  - See `docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md`.
  ```

**2. docs/adr/0013-ris-n8n-pilot-scoped.md** -- Lines 97-99

Replace lines 97-99:
```
- n8n 2.x provides instance-level MCP UI components (`/mcp-server/http` path rendered
  by the SPA). The HTTP MCP backend endpoint is an Enterprise feature — not available in
  the community edition. `N8N_MCP_BEARER_TOKEN` remains in compose but is informational.
```
with:
```
- n8n 2.x provides instance-level MCP at `/mcp-server/http`. The HTTP MCP backend
  works on community edition n8n >= 2.14.2 (not Enterprise-only). Authentication
  requires a JWT bearer token generated from n8n UI (Settings -> Instance-level MCP).
  `N8N_MCP_BEARER_TOKEN` is the compose-side env var read by n8n at container startup.
  Earlier probes incorrectly concluded "Enterprise-only" because GET requests without
  the required `Accept` header hit the SPA frontend router instead of the backend
  (see `docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md`).
```

**3. docs/RIS_OPERATOR_GUIDE.md** -- Line 500

Replace:
```
   - `N8N_MCP_BEARER_TOKEN` (not operative — MCP uses stdio, not HTTP; see note below)
```
with:
```
   - `N8N_MCP_BEARER_TOKEN` (compose-side env var read by n8n at startup; operative
     when instance-level MCP is enabled in n8n Settings UI; see MCP section below)
```

This resolves the internal contradiction: step 1 said "not operative" while the later
MCP section (lines 627+) correctly documented the token as operative. The later section
is the authoritative truth.

**4. docs/RIS_AUDIT_REPORT.md** -- Line 364

Replace:
```
| research-scheduler | IMPLEMENTED | start, stop, status, list subcommands |
```
with:
```
| research-scheduler | IMPLEMENTED | start, status, run-job subcommands |
```

The real CLI parser (tools/cli/research_scheduler.py lines 135-170) defines exactly
three subcommands: `status`, `start`, `run-job`. There is no `stop` or `list`. The
RIS_OPERATOR_GUIDE.md already correctly notes this at line 283:
"Note: `research-scheduler stop` and `research-scheduler list` are not implemented."

**IMPORTANT CONSTRAINTS:**
- Do NOT touch any code files, workflow JSON, docker-compose.yml, scripts/, or .claude/**
- Do NOT broaden any claim beyond "scoped RIS pilot"
- Do NOT change the scope boundaries in ADR 0013
- Preserve the distinction between scoped RIS pilot and broad Phase 3 automation
  </action>
  <verify>
    <automated>
Search all four touched docs for stale phrases. All of these must return 0 matches:

```bash
grep -c "Enterprise feature only" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md docs/RIS_OPERATOR_GUIDE.md
grep -c "informational placeholder" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md
grep -c "not operative" docs/RIS_OPERATOR_GUIDE.md
grep -c "start, stop, status, list" docs/RIS_AUDIT_REPORT.md
```

And confirm the corrected text is present:

```bash
grep -c "community edition" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md
grep -c "start, status, run-job" docs/RIS_AUDIT_REPORT.md
```
    </automated>
  </verify>
  <done>
All four docs agree on: (1) n8n instance-level MCP works on community edition >= 2.14.2,
(2) N8N_MCP_BEARER_TOKEN is operative (not informational), (3) research-scheduler has
exactly three subcommands: status, start, run-job. No internal contradictions remain
within RIS_OPERATOR_GUIDE.md regarding MCP/bearer-token truth.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dev log for this doc fix session</name>
  <files>docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md</files>
  <action>
Create the mandatory dev log at `docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md`.

Structure:

```markdown
# RIS n8n MCP Truth Fix -- 2026-04-06

**Quick ID:** 260406-nb7
**Scope:** Docs-only. No code, config, workflow JSON, docker-compose, or .claude changes.

## Why This Was Needed

Codex review found remaining contradictions after the prior doc passes (quick-260406-mnu,
quick-260406-le7, quick-260406-mno). Four operator-facing docs still contained stale
claims disproven by the MCP connection debug session (quick-260406-le7).

## Contradictions Found and Resolved

| File | Location | Old Statement | New Statement |
|------|----------|---------------|---------------|
| docs/CURRENT_STATE.md | Lines 1563-1564 | "Enterprise tier for backend endpoint" | "works on community edition; requires JWT bearer token" |
| docs/CURRENT_STATE.md | Lines 1572-1574 | "Enterprise feature only; not available in community edition" + "informational placeholder" | "works on community edition n8n >= 2.14.2 (not Enterprise-only)" + "compose-side env var read by n8n" |
| docs/adr/0013-ris-n8n-pilot-scoped.md | Lines 97-99 | "Enterprise feature -- not available in the community edition" + "informational" | "works on community edition n8n >= 2.14.2 (not Enterprise-only)" + "compose-side env var read by n8n" |
| docs/RIS_OPERATOR_GUIDE.md | Line 500 | "not operative -- MCP uses stdio, not HTTP" | "operative when instance-level MCP is enabled" |
| docs/RIS_AUDIT_REPORT.md | Line 364 | "start, stop, status, list subcommands" | "start, status, run-job subcommands" |

## Authoritative Truth Source

All corrections based on probe results from quick-260406-le7:
`docs/dev_logs/2026-04-06_n8n_instance_mcp_connection_debug.md`

Key facts:
- POST /mcp-server/http with Accept header + valid JWT -> 200 OK, MCP initialize response
- GET /mcp-server/http without Accept header -> 200 HTML (SPA catch-all, not the backend)
- This works on community edition n8n 2.14.2 (not Enterprise-only)
- research-scheduler real subcommands: status, start, run-job (no stop, no list)

## Files Changed

1. `docs/CURRENT_STATE.md` -- Fixed 2.x migration section; added le7 debug section
2. `docs/adr/0013-ris-n8n-pilot-scoped.md` -- Fixed image-and-versioning MCP paragraph
3. `docs/RIS_OPERATOR_GUIDE.md` -- Fixed step 1 bearer token description
4. `docs/RIS_AUDIT_REPORT.md` -- Fixed CLI table subcommand list
5. `docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md` -- This file

## Verification

[Insert grep results from Task 1 verify step here]

## What Was NOT Changed

- Code files, workflow JSON, docker-compose.yml, scripts/ -- out of scope
- .claude/** -- out of scope
- infra/n8n/README.md -- already correct (fixed in quick-260406-le7)
- .env.example -- already correct (fixed in quick-260406-le7)

## Codex Review

Tier: Skip (docs-only, no execution/strategy/risk logic).
```

Fill in the Verification section with actual grep outputs from Task 1.
  </action>
  <verify>
    <automated>test -f docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md && echo "Dev log exists"</automated>
  </verify>
  <done>Dev log exists with contradiction table, authoritative truth source, files changed list, and verification results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this is a docs-only change with no code, runtime, or
authentication surface modifications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-nb7-01 | I (Information Disclosure) | docs/adr/0013 | accept | ADR is a public repo doc; no secrets added. Bearer token values remain in .env (gitignored), not in docs. |
</threat_model>

<verification>
Run these commands after both tasks complete:

```bash
# Stale phrases must be gone (all return 0):
grep -c "Enterprise feature only" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md docs/RIS_OPERATOR_GUIDE.md
grep -c "informational placeholder" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md
grep -c "not operative" docs/RIS_OPERATOR_GUIDE.md
grep -c "start, stop, status, list" docs/RIS_AUDIT_REPORT.md

# Corrected phrases must be present:
grep -c "community edition" docs/CURRENT_STATE.md docs/adr/0013-ris-n8n-pilot-scoped.md
grep -c "start, status, run-job" docs/RIS_AUDIT_REPORT.md

# Dev log exists:
test -f docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md && echo "OK"

# Verify real CLI surface matches:
python -m polytool research-scheduler --help
```
</verification>

<success_criteria>
1. Zero grep hits for: "Enterprise feature only", "informational placeholder", "not operative", "start, stop, status, list" across all four docs
2. Positive grep hits for: "community edition" in CURRENT_STATE.md and ADR 0013
3. RIS_AUDIT_REPORT CLI table says "start, status, run-job" (not "start, stop, status, list")
4. RIS_OPERATOR_GUIDE step 1 says N8N_MCP_BEARER_TOKEN is "operative" (not "not operative")
5. Dev log exists at docs/dev_logs/2026-04-06_ris_n8n_mcp_truth_fix.md
6. No code, workflow JSON, docker-compose, scripts/, or .claude/** files were modified
</success_criteria>

<output>
After completion, create `.planning/quick/260406-nb7-eliminate-the-remaining-ris-n8n-document/260406-nb7-SUMMARY.md`
</output>
