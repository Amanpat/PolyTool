---
phase: quick-039
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CURRENT_STATE.md
  - docs/dev_logs/2026-03-28_gold_capture_campaign.md
  - artifacts/corpus_audit/shortage_report.md  # generated by corpus_audit.py
  - config/recovery_corpus_v1.tape_manifest    # written only if corpus qualifies
autonomous: false
requirements: [PHASE1B-GATE2]
must_haves:
  truths:
    - "Preflight confirms environment is ready (Docker/ClickHouse up, CLI loads, env vars set)"
    - "Path drift from quick-036 artifacts restructure is diagnosed and the correct --tape-roots for corpus_audit are established"
    - "At least one shadow capture session runs and completes (or a precise blocker is documented)"
    - "corpus_audit.py is run after capture and produces a shortage_report.md with current per-bucket counts"
    - "A final decision is recorded: GATE2_READY or MORE_GOLD_NEEDED with exact next steps"
    - "docs/CURRENT_STATE.md is updated with campaign outcome"
    - "dev log is written to docs/dev_logs/2026-03-28_gold_capture_campaign.md"
  artifacts:
    - path: "docs/dev_logs/2026-03-28_gold_capture_campaign.md"
      provides: "Campaign execution record with commands run, outcomes, and decision"
    - path: "docs/CURRENT_STATE.md"
      provides: "Updated Gate 2 corpus status"
    - path: "artifacts/corpus_audit/shortage_report.md"
      provides: "Authoritative per-bucket tape count after campaign"
  key_links:
    - from: "simtrader shadow --record-tape"
      to: "corpus_audit.py --tape-roots"
      via: "tape_dir written by shadow must be covered by at least one --tape-roots arg"
      pattern: "DEFAULT_ARTIFACTS_DIR.*tapes"
---

<objective>
Execute the Phase 1B Gold tape capture campaign to unblock Gate 2.

Purpose: Gate 2 is in NOT_RUN state because the corpus has only 10/50 qualifying tapes. The sole
blocker is corpus count — no code changes are needed. This plan runs the documented campaign
loop: preflight check, path drift audit, shadow recording sessions, eligibility audit, and a
final GATE2_READY or MORE_GOLD_NEEDED decision.

Output:
- Live Gold shadow tapes recorded to the canonical path
- corpus_audit.py run with correct --tape-roots to count newly admitted tapes
- Eligibility report (per-bucket counts, bucket-level progress)
- Final decision label with next command or next capture target
- dev log and CURRENT_STATE.md update
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Coding Projects/Polymarket/PolyTool/docs/CURRENT_STATE.md
@D:/Coding Projects/Polymarket/PolyTool/docs/runbooks/CORPUS_GOLD_CAPTURE_RUNBOOK.md
@D:/Coding Projects/Polymarket/PolyTool/docs/specs/SPEC-phase1b-gold-capture-campaign.md
@D:/Coding Projects/Polymarket/PolyTool/docs/dev_logs/2026-03-28_artifacts_restructure.md

<!-- Key path constants after quick-036 restructure -->
<interfaces>
From tools/gates/corpus_audit.py:
  DEFAULT_TAPE_ROOTS = [
    "artifacts/tapes/gold",
    "artifacts/tapes/silver",
    "artifacts/tapes",
  ]
  DEFAULT_OUT_DIR = "artifacts/corpus_audit"   # NOTE: NOT updated to artifacts/debug/corpus_audit
  DEFAULT_MIN_EVENTS = 50
  DEFAULT_MANIFEST_OUT = "config/recovery_corpus_v1.tape_manifest"

From tools/cli/simtrader.py:
  DEFAULT_ARTIFACTS_DIR = Path("artifacts/simtrader")
  # Shadow tape_dir = DEFAULT_ARTIFACTS_DIR / "tapes" / run_id
  # = artifacts/simtrader/tapes/<run_id>/
  # Print says "artifacts/tapes/shadow/<run_id>/" but ACTUAL WRITE is "artifacts/simtrader/tapes/<run_id>/"

CRITICAL PATH DRIFT: Shadow tapes land in artifacts/simtrader/tapes/ but
corpus_audit DEFAULT_TAPE_ROOTS does NOT include that path. When running
corpus_audit, you MUST pass --tape-roots artifacts/simtrader/tapes explicitly
(or verify the runbook's override roots are complete) so newly captured tapes
are scanned.

Shortage as of 2026-03-27:
  sports=15, politics=9, crypto=10, new_market=5, near_resolution=1
  Total needed: 40 more tapes
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Preflight audit and path drift check</name>
  <what-built>
    Run the following preflight commands in order. Document every result.
    Stop and document the exact failure if any command fails.

    STEP 1 — Environment check:
    ```
    docker compose ps
    curl "http://localhost:8123/?query=SELECT%201"
    echo $CLICKHOUSE_PASSWORD
    python -m polytool --help
    ```

    STEP 2 — Current corpus shortage:
    ```
    python tools/gates/capture_status.py
    ```
    This uses DEFAULT_TAPE_ROOTS (artifacts/tapes/gold, artifacts/tapes/silver, artifacts/tapes).
    Note the per-bucket counts printed.

    STEP 3 — Path drift check (shadow write path vs corpus_audit scan path):
    Run corpus_audit with the stale-but-complete roots that include the shadow write target:
    ```
    python tools/gates/corpus_audit.py \
        --tape-roots artifacts/simtrader/tapes \
        --tape-roots artifacts/tapes/gold \
        --tape-roots artifacts/tapes/silver \
        --tape-roots artifacts/tapes \
        --out-dir artifacts/corpus_audit \
        --manifest-out config/recovery_corpus_v1.tape_manifest
    ```
    Read artifacts/corpus_audit/shortage_report.md. Note whether count differs from Step 2.

    STEP 4 — Confirm shadow write path:
    Run a 10-second dry test to confirm shadow resolves and shows the tape-dir it would write to:
    ```
    python -m polytool simtrader shadow \
        --market will-btc-be-above-100k-on-march-28 \
        --strategy market_maker_v1 \
        --duration 10 \
        --record-tape 2>&1 | head -20
    ```
    (Expect "[shadow] tape dir : artifacts/simtrader/tapes/..." in stderr output.
     If market slug not found, use --list-candidates to pick an active slug.)

    STEP 5 — Identify best current capture targets:
    ```
    python -m polytool simtrader quickrun --list-candidates 20
    ```
    Note which candidates are in the highest-shortage bucket (sports > crypto > politics).
  </what-built>
  <how-to-verify>
    Preflight passes when ALL of:
    - docker compose ps shows all services healthy
    - curl returns "1"
    - CLICKHOUSE_PASSWORD is non-empty
    - python -m polytool --help shows no import errors
    - capture_status.py prints a bucket table without crashing
    - corpus_audit.py completes (exit 0 or 1 — either is fine)
    - shortage_report.md exists at artifacts/corpus_audit/shortage_report.md
    - The shadow tape-dir path is known (from Step 4 stderr)

    If any step fails, document the exact error and stop. Do NOT proceed to Task 2 until
    the blocker is diagnosed and the smallest fix is applied or documented.
  </how-to-verify>
  <resume-signal>
    Type "preflight-ok" when all five steps complete cleanly, or describe the exact
    blocker encountered (which command, what error, what was tried).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute Gold shadow capture sessions</name>
  <what-built>
    Run shadow recording sessions for the highest-shortage buckets. Use the shortage
    counts from Task 1 Step 3 to determine capture order.

    CAPTURE LOOP (repeat until corpus_audit exits 0 or session time exhausted):

    For each session, replace SLUG, BUCKET, and TIMESTAMP appropriately:

    ```
    python -m polytool simtrader shadow \
        --market <SLUG> \
        --strategy market_maker_v1 \
        --duration 600 \
        --record-tape
    ```

    NOTE: The shadow command auto-generates a tape dir under artifacts/simtrader/tapes/.
    Do NOT specify --tape-dir with a bucket prefix path from the old runbook examples.
    The auto-generated path is what corpus_audit must scan (via the explicit --tape-roots
    argument used in Task 1 Step 3).

    BUCKET TARGETING PRIORITY:
    1. sports (need >= 15): Use active NHL/NBA/soccer markets. Check Polymarket for
       live game markets. Example format: "nhl-team-a-vs-team-b-YYYY-MM-DD"
    2. crypto (need >= 10): BTC/ETH/SOL binary pairs. Use crypto-pair-watch if needed:
       python -m polytool crypto-pair-watch --one-shot
    3. politics (need >= 9): Active US political markets
    4. new_market (need >= 5): Recently listed markets (< 7 days old) from front page
    5. near_resolution (need >= 1): Any market resolving within 48h

    AFTER EACH SESSION — validate the tape immediately:
    ```
    python tools/gates/corpus_audit.py \
        --tape-roots artifacts/simtrader/tapes \
        --tape-roots artifacts/tapes/gold \
        --tape-roots artifacts/tapes/silver \
        --tape-roots artifacts/tapes \
        --out-dir artifacts/corpus_audit \
        --manifest-out config/recovery_corpus_v1.tape_manifest
    ```
    Read shortage_report.md. If the new tape was rejected (too_short), recapture
    with --duration 900 for that market.

    EFFECTIVE EVENTS CHECK (if a tape is rejected as too_short):
    ```
    python -c "
    from tools.gates.mm_sweep import _count_effective_events
    import pathlib
    print(_count_effective_events(pathlib.Path('artifacts/simtrader/tapes/<TAPE_DIR_NAME>/events.jsonl')))
    "
    ```

    IMPORTANT CONSTRAINTS (from SPEC-phase1b-gold-capture-campaign.md — non-negotiable):
    - min_events = 50 effective events required. Do NOT pass --min-events below 50.
    - benchmark_v1 is immutable. Do NOT touch config/benchmark_v1.* files.
    - Gate 2 threshold >= 70% is immutable.
    - Shadow mode never submits real orders — all sessions are safe.
  </what-built>
  <how-to-verify>
    After each capture session:
    - corpus_audit.py runs without crashing
    - shortage_report.md shows updated counts
    - Any newly captured tape that qualified has its bucket count incremented

    After all capture sessions for this work packet:
    - At least one tape was captured (or a precise market-availability blocker is documented)
    - The post-capture shortage_report.md exists with current counts
  </how-to-verify>
  <resume-signal>
    Type "capture-done" and paste the final per-bucket table from shortage_report.md,
    OR describe what blocked capture (no active markets found, WS error, etc.).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Produce eligibility report, update docs, write dev log</name>
  <files>
    docs/CURRENT_STATE.md
    docs/dev_logs/2026-03-28_gold_capture_campaign.md
  </files>
  <action>
    Using the shortage_report.md produced in Task 2 and the pre-campaign counts from
    Task 1, produce the eligibility summary and update docs.

    STEP 1 — Final corpus_audit run (authoritative):
    ```
    python tools/gates/corpus_audit.py \
        --tape-roots artifacts/simtrader/tapes \
        --tape-roots artifacts/tapes/gold \
        --tape-roots artifacts/tapes/silver \
        --tape-roots artifacts/tapes \
        --out-dir artifacts/corpus_audit \
        --manifest-out config/recovery_corpus_v1.tape_manifest
    FINAL_EXIT=$?
    echo "corpus_audit exit: $FINAL_EXIT"
    ```

    STEP 2 — Determine final decision:
    - If FINAL_EXIT == 0: decision = GATE2_READY
      Next command: python tools/gates/close_mm_sweep_gate.py --benchmark-manifest config/recovery_corpus_v1.tape_manifest --out artifacts/gates/mm_sweep_gate
    - If FINAL_EXIT == 1: decision = MORE_GOLD_NEEDED
      Read shortage_report.md and state: which buckets still need tapes, how many,
      and what the minimum next capture session set should be.

    STEP 3 — Write dev log at docs/dev_logs/2026-03-28_gold_capture_campaign.md:
    Include:
    - Pre-campaign corpus state (from Task 1: per-bucket counts before any captures)
    - Path drift finding: shadow tapes land in artifacts/simtrader/tapes/ not artifacts/tapes/shadow/;
      corpus_audit must include --tape-roots artifacts/simtrader/tapes to count them
    - Sessions run: market slug, bucket, duration, effective_events, accepted/rejected
    - Post-campaign corpus state: per-bucket table from shortage_report.md
    - Final decision: GATE2_READY or MORE_GOLD_NEEDED
    - If GATE2_READY: exact Gate 2 rerun command
    - If MORE_GOLD_NEEDED: minimum next capture targets (bucket, count needed, market type)
    - Blockers encountered (market availability, WS issues, etc.)
    - Test results: python -m pytest tests/test_mm_sweep_gate.py tests/test_mm_sweep_diagnostic.py -q

    STEP 4 — Update docs/CURRENT_STATE.md:
    Find the "Gate 2: NOT_RUN" section and update:
    - Change the tape count from "10/50" to the new count
    - Update "Shortage by bucket" line with post-campaign numbers
    - If GATE2_READY: change status to "corpus complete — Gate 2 rerun ready"
    - If MORE_GOLD_NEEDED: update shortage numbers and add campaign completion note
    - Update "Next executable step" sentence

    STEP 5 — Run regression tests:
    ```
    python -m pytest tests/test_mm_sweep_gate.py tests/test_mm_sweep_diagnostic.py tests/test_corpus_audit.py -q
    ```
    Report exact counts (N passed, 0 failed).
  </action>
  <verify>
    <automated>python -m pytest tests/test_mm_sweep_gate.py tests/test_mm_sweep_diagnostic.py -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - docs/dev_logs/2026-03-28_gold_capture_campaign.md exists with full campaign record
    - docs/CURRENT_STATE.md reflects post-campaign corpus state
    - Final decision is GATE2_READY or MORE_GOLD_NEEDED (explicitly stated)
    - If GATE2_READY: config/recovery_corpus_v1.tape_manifest exists and next command is named
    - If MORE_GOLD_NEEDED: per-bucket shortage and minimum next capture set is documented
    - Tests pass (no regressions in gate tooling)
  </done>
</task>

</tasks>

<verification>
End-to-end verification:

1. python tools/gates/capture_status.py — exits without error, prints bucket table
2. artifacts/corpus_audit/shortage_report.md — updated post-campaign
3. docs/dev_logs/2026-03-28_gold_capture_campaign.md — exists with GATE2_READY or MORE_GOLD_NEEDED heading
4. docs/CURRENT_STATE.md — Gate 2 section reflects current corpus count
5. python -m pytest tests/test_mm_sweep_gate.py tests/test_mm_sweep_diagnostic.py -q — all pass
</verification>

<success_criteria>
Campaign is complete when:
- At least one shadow session was attempted (or a documented market-availability blocker explains why zero sessions ran)
- corpus_audit.py ran post-campaign with --tape-roots including artifacts/simtrader/tapes
- shortage_report.md reflects post-campaign state
- Final decision is explicitly GATE2_READY or MORE_GOLD_NEEDED
- GATE2_READY means corpus_audit exits 0 and config/recovery_corpus_v1.tape_manifest exists
- MORE_GOLD_NEEDED means per-bucket gap is documented with minimum next target set
- All docs updated, dev log written, tests passing
</success_criteria>

<output>
After completion, create .planning/quick/39-gold-capture-campaign-for-phase-1b-gate-/39-SUMMARY.md
</output>
