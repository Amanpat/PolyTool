---
phase: 260410-ge0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/polymarket/discovery/__init__.py
  - docs/features/wallet-discovery-v1.md
  - docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md
autonomous: true
requirements: [WD-INTEGRATE]
must_haves:
  truths:
    - "Both commits 83832e1 (Packet A) and 724a23c (Packet B) are reachable from HEAD on main"
    - "All discovery module imports work together without ImportError guards"
    - "discovery CLI and scan --quick CLI both respond to --help"
    - "Combined touched-area test suite (106 tests) passes"
    - "Full regression baseline (3896 passed) is maintained"
  artifacts:
    - path: "packages/polymarket/discovery/__init__.py"
      provides: "Unified discovery package exports covering both Loop A models and MVF"
      contains: "compute_mvf"
    - path: "docs/features/wallet-discovery-v1.md"
      provides: "Feature doc updated to reflect implemented status"
      contains: "Implemented"
    - path: "docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md"
      provides: "Integration dev log with git state, commands, test results"
      contains: "106 passed"
  key_links:
    - from: "packages/polymarket/discovery/__init__.py"
      to: "packages/polymarket/discovery/mvf.py"
      via: "direct import of compute_mvf, MvfResult, mvf_to_dict"
      pattern: "from packages.polymarket.discovery.mvf import"
    - from: "tools/cli/scan.py"
      to: "packages/polymarket/discovery/mvf.py"
      via: "lazy import in _emit_trust_artifacts"
      pattern: "from packages.polymarket.discovery.mvf import compute_mvf"
    - from: "tools/cli/discovery.py"
      to: "packages/polymarket/discovery/loop_a.py"
      via: "lazy import in run-loop-a handler"
      pattern: "from packages.polymarket.discovery.loop_a import run_loop_a"
---

<objective>
Integrate Wallet Discovery v1 Packets A and B into a coherent combined state on main.

Purpose: Packets A (Loop A storage + plumbing, commit 83832e1) and B (MVF + scan --quick,
commit 724a23c) were implemented by parallel agents and pushed sequentially to main.
Both commits are already reachable from HEAD. This plan performs the integration cleanup:
resolving the redundant ImportError guard in __init__.py, updating package exports to
cover both packets, updating the feature doc to reflect implemented status, running the
combined test suite, and writing the mandatory dev log.

Output: Clean __init__.py, updated feature doc, dev log, green test suite.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/specs/SPEC-wallet-discovery-v1.md
@docs/dev_logs/2026-04-09_wallet_discovery_v1_impl_a.md
@docs/dev_logs/2026-04-09_wallet_discovery_v1_impl_b.md
@packages/polymarket/discovery/__init__.py
@packages/polymarket/discovery/mvf.py
@packages/polymarket/discovery/models.py
@tools/cli/scan.py
@tools/cli/discovery.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/polymarket/discovery/models.py:
- LifecycleState, ReviewStatus, QueueState (Enum classes)
- InvalidTransitionError, validate_transition
- WatchlistRow, LeaderboardSnapshotRow, ScanQueueRow (dataclasses)

From packages/polymarket/discovery/mvf.py:
- MvfResult (dataclass with 11 dimensions + metadata)
- compute_mvf(positions, wallet_address) -> MvfResult
- mvf_to_dict(result) -> dict (serializable output)

From packages/polymarket/discovery/loop_a.py:
- LoopAResult (dataclass)
- run_loop_a(...) -> LoopAResult
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify __init__.py and update feature doc</name>
  <files>packages/polymarket/discovery/__init__.py, docs/features/wallet-discovery-v1.md</files>
  <action>
1. **packages/polymarket/discovery/__init__.py** -- Perform three changes:
   a. Update the module docstring to describe the full v1 package (both Loop A plumbing
      AND MVF/scan-side), not just "Loop A plumbing". Keep it concise.
   b. Remove the try/except ImportError guard around the models import. Both models.py
      and mvf.py now exist on main -- the guard is dead code. Replace with a direct import
      block (no try/except).
   c. Add MVF exports: import `compute_mvf`, `MvfResult`, and `mvf_to_dict` from
      `packages.polymarket.discovery.mvf` and add them to `__all__`.

   The final __init__.py should have:
   - Direct imports from .models (all 8 existing symbols)
   - Direct imports from .mvf (compute_mvf, MvfResult, mvf_to_dict)
   - A single __all__ list covering all 11 symbols
   - No try/except ImportError

2. **docs/features/wallet-discovery-v1.md** -- Update status from "Spec frozen (2026-04-09).
   Implementation pending." to "Implemented (2026-04-09). Integrated (2026-04-10)." Add a
   brief "Implementation" section listing what was delivered:
   - Packet A (commit 83832e1): ClickHouse DDL, models, Loop A plumbing, discovery CLI, 54 tests
   - Packet B (commit 724a23c): MVF computation, scan --quick flag, 52 tests
   - Integration pass: unified package exports, combined 106 tests green

   Keep the existing What v1 Covers / What v1 Does NOT Cover sections unchanged.
   Update the CLI Surface section from "pending implementation" to remove the "(pending
   implementation)" parenthetical.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -c "from packages.polymarket.discovery import compute_mvf, MvfResult, mvf_to_dict, LifecycleState, validate_transition; print('All 11 exports OK')" && python -c "from packages.polymarket.discovery import __all__; assert len(__all__) == 11, f'Expected 11, got {len(__all__)}'; print('__all__ has 11 entries')"</automated>
  </verify>
  <done>
  - __init__.py has no try/except ImportError
  - __init__.py exports all 11 symbols (8 models + 3 MVF)
  - Feature doc status is "Implemented" not "pending"
  - CLI Surface section no longer says "pending implementation"
  </done>
</task>

<task type="auto">
  <name>Task 2: Run combined test suite + regression, write dev log</name>
  <files>docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md</files>
  <action>
1. Run the combined touched-area test suite:
   ```
   python -m pytest tests/test_wallet_discovery.py tests/test_mvf.py tests/test_scan_quick_mode.py -v --tb=short
   ```
   Expected: 106 passed (54 + 37 + 15).

2. Run CLI smoke checks:
   ```
   python -m polytool discovery --help
   python -m polytool scan --help | grep quick
   python -c "from packages.polymarket.discovery import __all__; print(__all__)"
   ```
   Expected: discovery help shows run-loop-a, scan help shows --quick, __all__ has 11 entries.

3. Run minimal regression slice (all tests except pre-existing failure):
   ```
   python -m pytest tests/ -q --tb=short -x --deselect tests/test_ris_phase2_cloud_provider_routing.py
   ```
   Expected: 3896 passed (or close to baseline), 0 new failures.

4. Create dev log at docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md with these sections:
   - **Task:** quick-260410-ge0 -- Integrate Wallet Discovery v1 Packets A and B
   - **Status:** COMPLETE
   - **Objective** (one paragraph)
   - **Git State Before**: HEAD is 83832e1 on main. Both 83832e1 (Packet A) and 724a23c
     (Packet B) already reachable -- no merge/rebase needed.
   - **What Was Changed**: List the 2 files modified and why (redundant guard removal,
     MVF export addition, feature doc status update)
   - **Commands Run + Output**: Exact commands and pass/fail counts from steps 1-3
   - **Test Results**: Touched-area: N passed. Regression: N passed, N pre-existing failures.
   - **Merge/Conflict Decisions**: None needed -- both commits were already linear on main
   - **Remaining Risks**: Note the pre-existing test_ris_phase2_cloud_provider_routing.py
     failure (8 tests, AttributeError on _post_json). Note that late_entry_rate MVF dimension
     returns null due to missing market timing fields (Gap E, expected per spec).
   - **Codex Review**: Skip -- no execution layer, no live-capital paths, no mandatory-tier files.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && python -m pytest tests/test_wallet_discovery.py tests/test_mvf.py tests/test_scan_quick_mode.py -q --tb=short && python -m polytool discovery --help > /dev/null 2>&1 && echo "SMOKE OK"</automated>
  </verify>
  <done>
  - 106 touched-area tests pass
  - CLI smoke checks pass (discovery --help, scan --quick visible)
  - Regression baseline maintained (3896+ passed, 0 new failures)
  - Dev log exists at docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md
  - Dev log contains git state, files changed, commands run, test results, conflict decisions, remaining risks
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is an integration/cleanup pass touching only
package exports and documentation. No new network calls, no new data paths, no new
CLI behavior.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ge0-01 | T (Tampering) | __init__.py exports | accept | Removing try/except makes imports fail-fast if either module is deleted -- this is safer than silent degradation. No new attack surface. |
</threat_model>

<verification>
1. `python -c "from packages.polymarket.discovery import compute_mvf, LifecycleState"` succeeds
2. `python -m polytool discovery --help` prints run-loop-a subcommand
3. `python -m polytool scan --help` includes --quick flag
4. `python -m pytest tests/test_wallet_discovery.py tests/test_mvf.py tests/test_scan_quick_mode.py` -- 106 passed
5. `python -m pytest tests/ -q --deselect tests/test_ris_phase2_cloud_provider_routing.py` -- 3896+ passed
6. docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md exists with required sections
</verification>

<success_criteria>
- Both Packet A (83832e1) and Packet B (724a23c) commits reachable from HEAD on main
- No ImportError guards in discovery __init__.py (dead code removed)
- Package exports cover both Loop A models (8 symbols) and MVF (3 symbols)
- Feature doc reflects "Implemented" status
- 106 touched-area tests green
- Regression baseline maintained
- Dev log written with full audit trail
</success_criteria>

<output>
After completion, create `.planning/quick/260410-ge0-integrate-wallet-discovery-v1-packets-a-/260410-ge0-SUMMARY.md`
</output>
