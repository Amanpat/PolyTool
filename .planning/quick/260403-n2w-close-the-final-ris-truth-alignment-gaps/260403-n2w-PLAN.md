---
phase: quick-260403-n2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CURRENT_STATE.md
  - docs/features/FEATURE-ris-dev-agent-integration-v1.md
  - docs/features/FEATURE-ris-v1-data-foundation.md
  - docs/features/wallet-scan-v0.md
  - docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md
autonomous: true
requirements: [RIS-v1-truth-alignment]
must_haves:
  truths:
    - "CURRENT_STATE.md does not list dossier auto-trigger or MCP KS routing as v2 deferred when both are shipped"
    - "CURRENT_STATE.md contains a section for quick-260403-lir (bridge CLI + MCP KS routing)"
    - "Feature docs do not contain stale 'not yet wired' or 'deferred' labels for shipped functionality"
    - "wallet-scan-v0.md command examples match actual CLI flags"
    - "RIS v1 COMPLETE closure section accurately reflects final shipped state"
  artifacts:
    - path: "docs/CURRENT_STATE.md"
      provides: "Accurate RIS v1 closure with no stale deferred claims"
    - path: "docs/features/FEATURE-ris-dev-agent-integration-v1.md"
      provides: "Updated v2 deferred list reflecting shipped dossier and MCP routing"
    - path: "docs/features/FEATURE-ris-v1-data-foundation.md"
      provides: "Updated status line and corrected deferred items"
    - path: "docs/features/wallet-scan-v0.md"
      provides: "Correct CLI flag names for dossier extraction"
    - path: "docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md"
      provides: "Closure dev log with exact changes and verification"
  key_links:
    - from: "docs/CURRENT_STATE.md"
      to: "shipped CLI commands"
      via: "wallet-scan --extract-dossier and research-register-hypothesis"
      pattern: "shipped.*extract-dossier|ks_active"
---

<objective>
Close the final RIS truth-alignment gaps in documentation so Codex can declare RIS v1 COMPLETE
without contradictions. Three plans shipped after the initial v1 closure section was written
(quick-260403-lim, quick-260403-lir, quick-260403-lix) and their shipped features are still
listed as "deferred" or "not yet wired" in multiple docs.

Purpose: Eliminate stale truth drift that makes the repo's documentation contradict itself.
Output: Corrected CURRENT_STATE.md, three feature docs, wallet-scan-v0.md, and closure dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/CURRENT_STATE.md
@docs/features/FEATURE-ris-dev-agent-integration-v1.md
@docs/features/FEATURE-ris-v1-data-foundation.md
@docs/features/wallet-scan-v0.md
@docs/dev_logs/2026-04-03_ris_final_truth_reconciliation.md
@docs/dev_logs/2026-04-03_ris_final_bridge_and_mcp_fix.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale deferred claims in CURRENT_STATE.md and add missing quick-260403-lir section</name>
  <files>docs/CURRENT_STATE.md</files>
  <action>
Make the following targeted edits to docs/CURRENT_STATE.md. Do NOT rewrite existing sections
that are already correct. Only touch the specific stale lines.

**Edit A — RIS v1 COMPLETE closure section (around line 1353-1379):**

In the "v2 Deferred" list:

1. REMOVE line "Auto-trigger dossier extraction after wallet-scan (hook not wired)" —
   this was shipped in quick-260403-lim via --extract-dossier flag. The Dossier
   Operationalization section at line 1323 already documents this correctly.

2. REMOVE line "MCP rag-query -> KnowledgeStore routing" — this was shipped in
   quick-260403-lir. The MCP tool now uses hybrid KS retrieval when the default
   DB exists (ks_active flag in response).

3. ADD to the "v1 Complete" list: "Bridge CLI (research-register-hypothesis,
   research-record-outcome) + MCP KnowledgeStore hybrid routing (quick-260403-lir)"

4. UPDATE test count from 3660 to 3689 (reflecting quick-260403-lir's 11 new tests
   plus quick-260403-lim's additions).

**Edit B — Line 1283 v2 deferred items under RIS_07 Dev Agent Integration section:**

Change: "Dossier-to-external-knowledge extraction (RIS_07 Section 1): **v1 shipped** —
CLI + batch extract via research-dossier-extract. Auto-trigger after wallet-scan
remains v2 deferred."

To: "Dossier-to-external-knowledge extraction (RIS_07 Section 1): **v1 shipped** —
CLI + batch extract via research-dossier-extract. Auto-trigger via --extract-dossier
also shipped (quick-260403-lim)."

**Edit C — Line 1286 under same section:**

Change: "MCP auto-routing for rag-query"

To: "MCP auto-routing for rag-query: **v1 shipped** — polymarket_rag_query uses
hybrid KS retrieval when DB exists (quick-260403-lir)"

**Edit D — Line 1314 under RIS R5 Dossier Pipeline section, Deferred list:**

The line already has an inline correction ("Wired via --extract-dossier flag").
Simplify to: "- Auto-trigger after wallet-scan: **Shipped** via --extract-dossier
flag (quick-260403-lim)"

**Edit E — Line 1368 (duplicate of the v2 deferred claim about auto-trigger):**

This is the same claim being removed in Edit A. Ensure it is gone.

**Edit F — Add quick-260403-lir section BEFORE the "RIS v1 — Complete" section (before line 1353):**

Add a new section:

```
## RIS Bridge CLI and MCP KnowledgeStore Routing (quick-260403-lir, 2026-04-03)

Closed the two final implementation gaps in RIS v1:

1. CLI entry points for `register_research_hypothesis` and `record_validation_outcome`
   (the core bridge logic existed but had no `polytool` commands).
2. `mcp_server.polymarket_rag_query` now uses hybrid KnowledgeStore retrieval when the
   default KS DB (`kb/rag/knowledge/knowledge.sqlite3`) is present. Falls back to
   vector-only when DB absent. Response includes `ks_active` bool.

**New/updated files:**
- `tools/cli/research_bridge.py` -- register-hypothesis + record-outcome subcommands
- `tools/cli/mcp_server.py` -- hybrid KS retrieval with ks_active flag
- `tests/test_ris_bridge_cli_and_mcp.py` -- 11 offline tests

11 new tests. 3689 total passing, 4 pre-existing failures unchanged.

See `docs/dev_logs/2026-04-03_ris_final_bridge_and_mcp_fix.md`.
```
  </action>
  <verify>
    <automated>python -c "
cs = open('docs/CURRENT_STATE.md').read()
# Check stale claims removed
assert 'hook not wired' not in cs.split('RIS v1')[1] if 'RIS v1' in cs else True, 'stale hook-not-wired in v1 closure'
assert 'MCP rag-query -> KnowledgeStore routing' not in cs.split('v2 Deferred')[1].split('##')[0] if 'v2 Deferred' in cs else True, 'stale MCP deferred'
# Check new section exists
assert 'quick-260403-lir' in cs, 'missing lir section'
assert 'ks_active' in cs, 'missing ks_active reference'
assert '3689' in cs, 'test count not updated'
print('CURRENT_STATE.md checks passed')
"</automated>
  </verify>
  <done>CURRENT_STATE.md no longer contains stale deferred claims for shipped features.
  quick-260403-lir section exists. RIS v1 COMPLETE list includes bridge CLI + MCP routing.
  v2 deferred list only contains genuinely unshipped items.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale claims in feature docs and wallet-scan-v0.md</name>
  <files>docs/features/FEATURE-ris-dev-agent-integration-v1.md, docs/features/FEATURE-ris-v1-data-foundation.md, docs/features/wallet-scan-v0.md</files>
  <action>
**FEATURE-ris-dev-agent-integration-v1.md edits:**

1. Line 170-172: The dossier extraction bullet says "Auto-extract key findings from
   wallet dossiers into external_knowledge partition. Requires LLM extraction prompt
   and integration with wallet-scan / alpha-distill."
   UPDATE to: "Dossier-to-external-knowledge extraction: **v1 shipped** (quick-260403-jy8,
   quick-260403-lim). research-dossier-extract CLI handles batch extraction. wallet-scan
   --extract-dossier auto-triggers after each scan. **v2 remaining:** LLM-assisted
   extraction (authority conflict blocks this) and alpha-distill integration."

2. Lines 186-188: MCP bullet says "The existing MCP tool queries Chroma but does not
   include the KnowledgeStore as a retrieval source."
   UPDATE to: "MCP polymarket_rag_query KnowledgeStore routing: **v1 shipped**
   (quick-260403-lir). The MCP tool now uses hybrid KS retrieval when the default DB
   exists (ks_active flag in response). **v2 remaining:** none for basic routing."

**FEATURE-ris-v1-data-foundation.md edits:**

1. Line 4 status: Change "Complete (data-plane only; not yet wired into Chroma query path)"
   to "Complete (data-plane + query spine wired via quick-260402-ivb, MCP routing
   via quick-260403-lir)"

2. Lines 132-134 "Deliberate Simplifications" bullet about Chroma: Change
   "No Chroma integration yet. This is a data-plane addition. The knowledge store
   is not wired into the existing Chroma/FTS5 hybrid retrieval pipeline. That
   wiring is deferred to a future plan."
   to "Chroma/FTS5 hybrid retrieval wiring shipped in quick-260402-ivb (query spine)
   and quick-260403-lir (MCP KnowledgeStore routing with ks_active flag). The
   original data-plane-only limitation is resolved."

3. Lines 408-409 "Deferred" in Phase R5 section: Change "Auto-trigger after
   wallet-scan: calling research-dossier-extract automatically at the end of
   wallet-scan is not yet wired. Run manually after each scan session."
   to "Auto-trigger after wallet-scan: **Shipped** via --extract-dossier flag
   on wallet-scan (quick-260403-lim). Run manually or pass --extract-dossier
   for automatic extraction after each per-wallet scan."

**wallet-scan-v0.md edits:**

1. Line 152: Change `--base-dir` to `--dossier-base`:
   Before: `python -m polytool research-dossier-extract --batch --base-dir artifacts/dossiers/users`
   After:  `python -m polytool research-dossier-extract --batch --dossier-base artifacts/dossiers/users`

Verify all other command examples in wallet-scan-v0.md are accurate against
`python -m polytool wallet-scan --help`. The existing examples use `--input`,
`--profile`, `--out`, `--max-entries`, `--extract-dossier`, `--extract-dossier-db`
which all match the actual CLI surface.
  </action>
  <verify>
    <automated>python -c "
# Check FEATURE-ris-dev-agent-integration-v1.md
f1 = open('docs/features/FEATURE-ris-dev-agent-integration-v1.md').read()
assert 'not yet wired to KS' not in f1, 'stale MCP claim in dev-agent feature doc'
assert 'v1 shipped' in f1.split('Dossier-to-external')[1][:200] if 'Dossier-to-external' in f1 else True, 'dossier not marked shipped'

# Check FEATURE-ris-v1-data-foundation.md
f2 = open('docs/features/FEATURE-ris-v1-data-foundation.md').read()
assert 'not yet wired into Chroma query path' not in f2, 'stale Chroma claim in data foundation doc'
assert 'not yet wired. Run manually' not in f2, 'stale auto-trigger claim'

# Check wallet-scan-v0.md
f3 = open('docs/features/wallet-scan-v0.md').read()
assert '--base-dir' not in f3, 'stale --base-dir flag in wallet-scan doc'
assert '--dossier-base' in f3, 'missing correct --dossier-base flag'

print('All feature doc checks passed')
"</automated>
  </verify>
  <done>All three feature docs corrected. No stale "not yet wired" or "deferred" labels
  remain for shipped functionality. wallet-scan-v0.md command examples match actual CLI.
  v2 deferred items are only genuinely unshipped items.</done>
</task>

<task type="auto">
  <name>Task 3: Create closure dev log and run smoke tests</name>
  <files>docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md</files>
  <action>
Create docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md with:

```markdown
# Dev Log: RIS Final Truth-Drift Cleanup

**Date:** 2026-04-03
**Plan:** quick-260403-n2w
**Type:** Documentation-only (truth alignment, zero code changes)

---

## Objective

Close the final RIS truth-alignment gaps so that CURRENT_STATE.md, feature docs,
and wallet-scan docs no longer contain stale deferred claims for shipped behavior.
Three plans shipped after the initial RIS v1 closure section was written
(quick-260403-lim, quick-260403-lir, quick-260403-lix) creating truth drift.

---

## Stale Claims Corrected

### CURRENT_STATE.md

| Line area | Stale claim | Correction |
|-----------|-------------|------------|
| v2 Deferred list | "Auto-trigger dossier extraction after wallet-scan (hook not wired)" | REMOVED — shipped in quick-260403-lim via --extract-dossier |
| v2 Deferred list | "MCP rag-query -> KnowledgeStore routing" | REMOVED — shipped in quick-260403-lir with ks_active flag |
| v1 Complete list | Missing bridge CLI + MCP routing | ADDED bridge CLI + MCP KS routing entry |
| Test count | 3660 | Updated to 3689 |
| Missing section | No entry for quick-260403-lir | ADDED full section before RIS v1 COMPLETE |
| RIS_07 deferred bullets | Auto-trigger "remains v2 deferred" | Updated to "also shipped" |
| RIS_07 deferred bullets | "MCP auto-routing for rag-query" bare deferred | Updated to "v1 shipped" |

### FEATURE-ris-dev-agent-integration-v1.md

| Line area | Stale claim | Correction |
|-----------|-------------|------------|
| v2 Deferred, dossier bullet | Fully deferred (requires LLM + wallet-scan integration) | Updated: v1 shipped, only LLM extraction remains v2 |
| v2 Deferred, MCP bullet | "not yet wired to KS" | Updated: v1 shipped via quick-260403-lir |

### FEATURE-ris-v1-data-foundation.md

| Line area | Stale claim | Correction |
|-----------|-------------|------------|
| Status line (line 4) | "not yet wired into Chroma query path" | Updated: query spine + MCP routing shipped |
| Deliberate Simplifications | "No Chroma integration yet" | Updated: wiring shipped |
| Phase R5 Deferred | "Auto-trigger not yet wired" | Updated: shipped via --extract-dossier |

### wallet-scan-v0.md

| Line area | Stale claim | Correction |
|-----------|-------------|------------|
| Line 152 | `--base-dir` flag name | Changed to `--dossier-base` (matches actual CLI) |

---

## Commands Run

[To be filled during execution with actual CLI smoke outputs]

---

## Final RIS v1 State

### Shipped (v1 complete)

All RIS subsystems are shipped. The full v1 surface includes:
- R0: Knowledge store foundation
- R1: Academic ingestion (ArXiv, BookAdapter, manual URL)
- R2: Social ingestion (Reddit, YouTube)
- R3: Synthesis engine (deterministic)
- R4: Infrastructure (scheduler, health, stats, report catalog)
- R5: Dossier pipeline + wallet-scan --extract-dossier auto-trigger
- Dev agent integration (CLAUDE.md section, operator recipes)
- SimTrader bridge (research-register-hypothesis, research-record-outcome)
- Bridge CLI + MCP KnowledgeStore hybrid routing (ks_active)

### Genuinely v2 Deferred

- Auto-discovery -> knowledge loop (requires candidate scanner integration)
- SimTrader auto-promotion loop (bridge shipped; auto-loop not wired)
- LLM-based synthesis (DeepSeek V3 prose generation)
- n8n migration from APScheduler
- ClickHouse ingestion_log table + Grafana panels
- ChatGPT architect / Google Drive connector
- Weekly digest automation
- SSRN ingestion, Twitter/X ingestion
- LLM-assisted dossier memo extraction (authority conflict)

---

## Codex Review

**Tier:** Docs-only changes. Skip tier. No adversarial review required.
```

After creating the dev log, run these smoke tests and record the output:
1. `python -m polytool wallet-scan --help` — verify --extract-dossier and --extract-dossier-db appear
2. `python -m polytool research-dossier-extract --help` — verify --dossier-base (not --base-dir)
3. `python -m polytool research-register-hypothesis --help` — verify command exists
4. `python -m polytool research-record-outcome --help` — verify command exists
5. `python -m polytool --help` — verify CLI still loads

Update the "Commands Run" section of the dev log with the actual outputs from
these smokes (just first 3-5 lines of each, enough to confirm the command loads).
  </action>
  <verify>
    <automated>python -c "
import os
assert os.path.exists('docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md'), 'dev log missing'
dl = open('docs/dev_logs/2026-04-03_ris_final_truth_drift_cleanup.md').read()
assert 'quick-260403-n2w' in dl, 'plan ID missing from dev log'
assert 'Stale Claims Corrected' in dl, 'corrections table missing'
assert 'v2 Deferred' in dl, 'v2 deferred list missing'
print('Dev log checks passed')
"</automated>
  </verify>
  <done>Dev log created with exact stale claims removed/corrected, smoke test outputs recorded,
  final v1 complete vs v2 deferred statement accurate. All CLI smoke tests pass.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Grep CURRENT_STATE.md for "hook not wired" — should return 0 matches in the RIS v1 closure section
2. Grep CURRENT_STATE.md for "MCP rag-query -> KnowledgeStore routing" in v2 deferred — should return 0 matches
3. Grep all feature docs for "not yet wired" — should return 0 matches
4. Grep wallet-scan-v0.md for "--base-dir" — should return 0 matches
5. Verify quick-260403-lir section exists in CURRENT_STATE.md
6. All CLI smoke tests pass (commands load without import errors)
</verification>

<success_criteria>
- Zero stale deferred claims remain in CURRENT_STATE.md for features shipped in quick-260403-lim/lir/lix
- FEATURE-ris-dev-agent-integration-v1.md v2 deferred list only contains genuinely unshipped items
- FEATURE-ris-v1-data-foundation.md status line and simplifications section reflect shipped query wiring
- wallet-scan-v0.md command examples match actual CLI flag names
- Dev log documents every stale claim corrected with before/after
- All existing tests still pass (no code changes, doc-only plan)
</success_criteria>

<output>
After completion, create `.planning/quick/260403-n2w-close-the-final-ris-truth-alignment-gaps/260403-n2w-SUMMARY.md`
</output>
