---
phase: quick-260403-o5h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md
  - docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md
autonomous: true
requirements: [RIS-truth-cleanup]

must_haves:
  truths:
    - "Every operator-facing rag-query example that uses --knowledge-store also includes --hybrid"
    - "Historical 'old text' sections in dev logs are left untouched"
  artifacts:
    - path: "docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md"
      provides: "Corrected operator example"
      contains: "--hybrid --knowledge-store default"
    - path: "docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md"
      provides: "Dev log documenting the fix"
      min_lines: 10
  key_links: []
---

<objective>
Fix the last RIS truth-drift blocker: the operator-facing rag-query example in
`docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md` uses
`--knowledge-store default` without `--hybrid`, which fails at runtime.

Purpose: Eliminate the final known stale operator example so all RIS docs are accurate.
Output: One corrected dev log + one new mini dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md

The stale example is on line 167 of the dossier operationalization dev log, inside
the "What the first-class dossier flow now looks like" > "Operator flow" code block:

```bash
python -m polytool rag-query --question "MOMENTUM strategy wallets" --knowledge-store default
```

This must become:

```bash
python -m polytool rag-query --question "MOMENTUM strategy wallets" --hybrid --knowledge-store default
```

Scan results confirm this is the ONLY live operator-facing instance. Other occurrences:
- `2026-04-03_wallet_scan_truth_drift_fix.md` line 38: inside a "Old text (broken at runtime)"
  historical section — leave it alone.
- `2026-04-03_ris_final_dossier_queryability_fix.md` line 108: already correct (has --hybrid).
- `2026-04-03_ris_final_truth_reconciliation.md` line 41: already correct (has --hybrid).
- `CLAUDE.md` line 387: already correct (has --hybrid).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale rag-query example and write dev log</name>
  <files>
    docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md,
    docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md
  </files>
  <action>
1. Open `docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md`.
2. Find line 167 (inside the "Operator flow" code block under "What the first-class dossier flow now looks like"):
   ```
   python -m polytool rag-query --question "MOMENTUM strategy wallets" --knowledge-store default
   ```
3. Replace with:
   ```
   python -m polytool rag-query --question "MOMENTUM strategy wallets" --hybrid --knowledge-store default
   ```
   This adds `--hybrid` before `--knowledge-store default`, matching the runtime requirement
   confirmed in `tools/cli/rag_query.py` lines 210-212.
4. Do NOT modify any other line in this file. Specifically do NOT touch:
   - The "What remains explicitly deferred" section
   - Any historical or "old text" quotes in other dev logs
5. Create `docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md` with:
   - Date: 2026-04-03
   - Task: quick-260403-o5h
   - Objective: one-line note that the stale rag-query example was fixed
   - What changed: line 167 of `2026-04-03_ris_final_dossier_operationalization.md`
     now includes `--hybrid` before `--knowledge-store default`
   - Why: `--knowledge-store` requires `--hybrid` at runtime; omitting it causes
     `Error: --knowledge-store requires --hybrid mode.`
   - Scope: doc-only, no code changes
  </action>
  <verify>
    <automated>grep -n "hybrid.*knowledge-store\|knowledge-store.*hybrid" "docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md" | head -5 && grep -c "knowledge-store" "docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md" && echo "--- checking no stale examples remain ---" && grep -n "\-\-knowledge-store" "docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md" | grep -v "\-\-hybrid" | grep -v "^$" || echo "ALL CLEAR: every --knowledge-store usage includes --hybrid"</automated>
  </verify>
  <done>
    - Line 167 of `2026-04-03_ris_final_dossier_operationalization.md` reads
      `python -m polytool rag-query --question "MOMENTUM strategy wallets" --hybrid --knowledge-store default`
    - No instance of `--knowledge-store` without `--hybrid` remains in that file
    - `docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md` exists documenting the fix
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "knowledge-store" docs/dev_logs/2026-04-03_ris_final_dossier_operationalization.md` shows only lines that also contain `--hybrid`
2. `docs/dev_logs/2026-04-03_ris_final_one_line_truth_cleanup.md` exists and is non-empty
3. No code files were modified
</verification>

<success_criteria>
Every operator-facing `rag-query --knowledge-store` example in the RIS dev logs includes `--hybrid`.
A dev log documents this one-line fix for traceability.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-o5h-remove-the-final-ris-truth-blocker-fix-r/260403-o5h-SUMMARY.md`
</output>
