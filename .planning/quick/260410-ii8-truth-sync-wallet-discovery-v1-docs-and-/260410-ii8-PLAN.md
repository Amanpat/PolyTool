---
phase: quick-260410-ii8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CURRENT_STATE.md
  - docs/ROADMAP.md
  - docs/INDEX.md
  - docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md
  - docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "CURRENT_STATE.md no longer says Wallet Discovery v1 implementation is pending"
    - "ROADMAP.md checklist items for Wallet Discovery v1 are checked and status reflects shipped"
    - "INDEX.md Wallet Discovery v1 row reflects implemented status and the new runbook is listed"
    - "A release-readiness runbook exists at docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"
    - "Grep for 'implementation.*pending' or 'SPEC FROZEN' tied to Wallet Discovery returns zero hits in the touched docs"
  artifacts:
    - path: "docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"
      provides: "Release-readiness checklist and research workflow runbook for Wallet Discovery v1"
    - path: "docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md"
      provides: "Mandatory dev log for this truth-sync packet"
  key_links:
    - from: "docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"
      to: "docs/specs/SPEC-wallet-discovery-v1.md"
      via: "cross-reference"
    - from: "docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"
      to: "docs/features/wallet-discovery-v1.md"
      via: "cross-reference"
---

<objective>
Truth-sync all authoritative docs so Wallet Discovery v1 is no longer described as
"pending" or "spec frozen (implementation pending)," and create a concise release-readiness
runbook for using v1 in research workflows.

Purpose: The feature is shipped, integrated, and hardened (118 tests). Three docs still
describe it as pending. Operators need a single go/no-go checklist before using discovery
in research workflows.

Output: Updated CURRENT_STATE.md, ROADMAP.md, INDEX.md, new runbook, dev log.
</objective>

<execution_context>
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/workflows/execute-plan.md
@D:/Coding Projects/Polymarket/PolyTool/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/specs/SPEC-wallet-discovery-v1.md (frozen contract -- read-only reference, do NOT modify)
@docs/features/wallet-discovery-v1.md (already accurate -- reference for shipped scope)
@docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md (integration pass record)
@docs/dev_logs/2026-04-10_wallet_discovery_v1_acceptance_hardening.md (hardening pass record)
@docs/CURRENT_STATE.md (lines 31-43 need update)
@docs/ROADMAP.md (lines 518-530 need update)
@docs/INDEX.md (line 80 needs update, runbook row needs adding)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CURRENT_STATE.md, ROADMAP.md, and INDEX.md to reflect shipped v1</name>
  <files>docs/CURRENT_STATE.md, docs/ROADMAP.md, docs/INDEX.md</files>
  <action>
Update three docs to reflect Wallet Discovery v1 as shipped/integrated/hardened:

**docs/CURRENT_STATE.md** (lines 31-43):
- Change section heading from "Wallet Discovery v1 (Spec Frozen, 2026-04-09)" to
  "Wallet Discovery v1 (Shipped, 2026-04-10)"
- Replace "Wallet Discovery v1 contract is frozen as a docs-only spec." and
  "Implementation is pending." with text stating:
  Wallet Discovery v1 is implemented, integrated, and hardened.
  Shipped across two parallel packets (commits 83832e1, 724a23c) with integration
  (2026-04-10) and acceptance hardening (12 integrated tests, 118 total discovery-area
  tests, 3908 full suite).
- Keep the scope description (Loop A, ClickHouse tables, scan --quick, MVF) as-is.
- Keep the "Full four-loop discovery system (B/C/D)..." deferred paragraph as-is.
- Add a line: "**Runbook**: docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"
- Keep the existing Spec and Feature doc links.

**docs/ROADMAP.md** (lines 518-530):
- Change section heading from "### Wallet Discovery v1 [SPEC FROZEN]" to
  "### Wallet Discovery v1 [SHIPPED]"
- Check all four checklist items (change `- [ ]` to `- [x]`):
  - Loop A: leaderboard fetcher + churn detection + scan queue
  - ClickHouse tables: watchlist, leaderboard_snapshots, scan_queue
  - Unified `polytool scan <address>` with `--quick` (no-LLM guarantee)
  - MVF computation (11-dim, Python math only)
- After the checklist, add: "**Shipped**: 2026-04-10. 118 discovery-area tests passing. 3908 full suite."
- Keep the Spec link, Acceptance line, and Deferred block unchanged.

**docs/INDEX.md** (line 80, Features table):
- Change the Wallet Discovery v1 row description from
  "Spec frozen: Loop A leaderboard discovery, watchlist/queue/snapshot ClickHouse tables, unified scan --quick, MVF computation"
  to
  "Shipped: Loop A leaderboard discovery, watchlist/queue/snapshot ClickHouse tables, unified scan --quick, MVF computation"
- Add a new row in the Workflows table (after the RIS Discord Alerts row) for the new runbook:
  `| [Wallet Discovery v1 Runbook](runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md) | Go/no-go readiness checklist and research workflow guide for Wallet Discovery v1 |`

**What NOT to do:**
- Do NOT modify docs/specs/SPEC-wallet-discovery-v1.md (frozen spec is read-only).
- Do NOT modify docs/features/wallet-discovery-v1.md (already accurate).
- Do NOT change any policy language in PLAN_OF_RECORD.
- Do NOT imply Loop B/C/D, insider scoring, auto-promotion, or n8n discovery are shipped.
  </action>
  <verify>
    <automated>cd "D:/Coding Projects/Polymarket/PolyTool" && grep -n "implementation.*pending\|Implementation is pending\|SPEC FROZEN" docs/CURRENT_STATE.md docs/ROADMAP.md docs/INDEX.md; echo "EXIT: $?"</automated>
    The grep should return zero matching lines. EXIT code may be 1 (no matches found) which is the desired outcome.
    Also verify: `grep -c "\- \[x\]" docs/ROADMAP.md | head -1` shows the 4 new checks exist (count increased).
  </verify>
  <done>
    - CURRENT_STATE.md Wallet Discovery v1 section says "Shipped" not "pending"
    - ROADMAP.md has all 4 items checked and heading says [SHIPPED]
    - INDEX.md row says "Shipped:" and a new Workflows row points to the runbook
    - No stale "implementation pending" or "[SPEC FROZEN]" language in touched docs
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wallet Discovery v1 release-readiness runbook</name>
  <files>docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md</files>
  <action>
Create docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md following the existing runbook conventions
(see docs/runbooks/ peers for formatting style). The document must contain exactly these sections:

**Title**: Wallet Discovery v1 — Release Readiness and Research Runbook

**What Is Shipped** section:
- Loop A leaderboard discovery (24h scheduled fetch, churn detection, scan queue population)
- ClickHouse table contracts: watchlist (ReplacingMergeTree), leaderboard_snapshots (MergeTree append-only), scan_queue (ReplacingMergeTree with lease semantics)
- Unified `python -m polytool scan <address> --quick` (hard no-LLM guarantee)
- MVF (Multi-Variate Fingerprint): 11-dimensional deterministic vector from trade history
- Wallet lifecycle state machine (discovered -> queued -> scanned -> reviewed -> promoted)
- 118 passing tests (54 Loop A + 37 MVF + 15 scan-quick + 12 integrated acceptance)

**What Is Explicitly Not Shipped** section:
- Loop B (live wallet monitoring via Alchemy WebSocket)
- Loop C (deep analysis + cloud LLM hypothesis generation)
- Loop D (platform-wide anomaly detection)
- Insider scoring (binomial test identified as mathematically incorrect in architect review)
- Auto-promotion to watchlist (human review gate mandatory)
- Cloud LLM calls for wallet analysis (policy RIS-only unless PLAN_OF_RECORD updated)
- n8n workflow integration for discovery (Phase 3 target)
- Note: See docs/specs/SPEC-wallet-discovery-v1.md "Blockers for Phases Beyond v1" for exact prerequisites

**Prerequisites** section:
- ClickHouse running and accessible (check: `curl "http://localhost:8123/?query=SELECT%201"`)
- Tables created (DDL in docs/specs/SPEC-wallet-discovery-v1.md ClickHouse Table Contracts section)
- CLICKHOUSE_PASSWORD env var set (per CLAUDE.md auth rule -- no hardcoded fallbacks)
- Python environment with polytool installed (`python -m polytool --help` loads without errors)

**CLI Entrypoints** section (exact commands):
```
# Run Loop A leaderboard fetch + churn detection
python -m polytool discovery run-loop-a

# Quick scan a wallet (no LLM calls, produces MVF + detectors + PnL)
python -m polytool scan <wallet_address> --quick

# Standard scan (unchanged existing behavior)
python -m polytool scan <wallet_address>
```

**No-LLM Guarantee** section:
- `scan --quick` makes zero HTTP calls to any cloud LLM endpoint (Gemini, DeepSeek, OpenAI, Anthropic)
- This is enforced by test AT-06 (request-intercepting fixtures, not inspection)
- Without `--quick`, existing scan behavior is unchanged (may use LLM if configured)

**Human Review Gate** section:
- A human review gate is required before any wallet reaches `promoted` or `watched` state
- The lifecycle state machine rejects `scanned -> promoted` (must go through `reviewed`)
- `reviewed -> promoted` requires `review_status = 'approved'` set by human operator
- No code path in v1 bypasses this gate

**Known Non-Blocking Issues** section:
- Pre-existing `test_ris_phase2_cloud_provider_routing.py` failures (8 tests, AttributeError on `_post_json`) -- unrelated to Wallet Discovery v1, owned by RIS Phase 2 cloud provider routing feature
- `late_entry_rate` MVF dimension returns null when `market_open_ts` / `close_timestamp` fields are absent from dossier export (documented as Gap E in spec; returning null is correct per spec)

**Go/No-Go Checklist** section:
```
[ ] ClickHouse responding: curl "http://localhost:8123/?query=SELECT%201" returns "1"
[ ] DDL applied: all 3 tables exist (watchlist, leaderboard_snapshots, scan_queue)
[ ] CLICKHOUSE_PASSWORD set in environment
[ ] CLI loads: python -m polytool --help shows discovery and scan commands
[ ] Discovery area tests pass: python -m pytest tests/test_wallet_discovery.py tests/test_mvf.py tests/test_scan_quick_mode.py tests/test_wallet_discovery_integrated.py -v --tb=short
[ ] Operator understands: no auto-promotion; human review gate is mandatory
[ ] Operator understands: scan --quick has no LLM calls; scan without --quick is unchanged
```

**Cross-References** section:
- Spec: docs/specs/SPEC-wallet-discovery-v1.md
- Feature doc: docs/features/wallet-discovery-v1.md
- Integration dev log: docs/dev_logs/2026-04-10_wallet_discovery_v1_integration.md
- Hardening dev log: docs/dev_logs/2026-04-10_wallet_discovery_v1_acceptance_hardening.md
  </action>
  <verify>
    <automated>test -f "D:/Coding Projects/Polymarket/PolyTool/docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md" && echo "RUNBOOK EXISTS" && grep -c "Go/No-Go\|What Is Shipped\|What Is Explicitly Not\|Prerequisites\|CLI Entrypoints\|No-LLM Guarantee\|Human Review Gate\|Known Non-Blocking" "D:/Coding Projects/Polymarket/PolyTool/docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md"</automated>
    File exists and contains all 8 required section headers (count should be >= 8).
  </verify>
  <done>
    - Runbook file exists at docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md
    - Contains all 8 sections: shipped scope, not-shipped, prerequisites, CLI entrypoints, no-LLM guarantee, human review gate, known issues, go/no-go checklist
    - Does not imply any deferred capability is shipped
    - Does not expand policy beyond current PLAN_OF_RECORD
  </done>
</task>

<task type="auto">
  <name>Task 3: Write mandatory dev log and verify all docs agree</name>
  <files>docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md</files>
  <action>
Create docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md with:

**Title**: 2026-04-10 — Wallet Discovery v1 Truth Sync and Release Checklist

**Task**: quick-260410-ii8

**Status**: COMPLETE

**Objective**: Truth-sync authoritative docs so Wallet Discovery v1 is no longer described
as "pending" and create a release-readiness runbook for research workflows.

**What Was Changed** section — list each file touched and what changed:
1. docs/CURRENT_STATE.md — section heading and body updated from "Spec Frozen / Implementation pending" to "Shipped" with integration and hardening details
2. docs/ROADMAP.md — heading changed from [SPEC FROZEN] to [SHIPPED], all 4 checklist items checked, shipped date and test count added
3. docs/INDEX.md — feature row updated from "Spec frozen" to "Shipped", new Workflows row added for the runbook
4. docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md — new file: release-readiness checklist and research workflow guide

**What Was NOT Changed** section:
- docs/specs/SPEC-wallet-discovery-v1.md — frozen spec, not modified (still says "Implementation is pending" in its own Status section; the feature doc is the live status source)
- docs/features/wallet-discovery-v1.md — already accurate from integration and hardening passes
- docs/PLAN_OF_RECORD.md — no policy changes
- No code, migrations, tests, or workflow changes

**Verification** section:
- Include the exact grep command and output showing zero "implementation pending" hits in the touched docs
- Note the frozen spec intentionally retains its original status wording (it is a point-in-time contract snapshot, not a living status doc)

**Codex Review**: Skip — docs-only, no execution layer touched.
  </action>
  <verify>
    <automated>test -f "D:/Coding Projects/Polymarket/PolyTool/docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md" && echo "DEV LOG EXISTS"</automated>
  </verify>
  <done>
    - Dev log exists with task ID, objective, changed files, unchanged files, and verification output
    - All touched docs agree with the frozen spec and the hardening dev log
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed -- this is a docs-only task with no code, no data writes,
no API calls, and no infrastructure changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ii8-01 | Tampering | docs/PLAN_OF_RECORD.md | accept | Task explicitly does not touch PLAN_OF_RECORD; verify in dev log |
| T-ii8-02 | Information Disclosure | Runbook | accept | Runbook contains only public CLI commands and ClickHouse DDL references already in committed spec; no secrets |
</threat_model>

<verification>
Run after all tasks complete:

```bash
# 1. No stale "implementation pending" language in touched docs
grep -rn "implementation.*pending\|Implementation is pending" docs/CURRENT_STATE.md docs/ROADMAP.md docs/INDEX.md
# Expected: no output (exit 1)

# 2. No stale "[SPEC FROZEN]" in ROADMAP for Wallet Discovery
grep -n "SPEC FROZEN" docs/ROADMAP.md
# Expected: no output (exit 1)

# 3. Runbook exists and has all required sections
grep -c "Go/No-Go\|What Is Shipped\|What Is Explicitly Not\|Prerequisites\|CLI Entrypoints\|No-LLM Guarantee\|Human Review Gate\|Known Non-Blocking" docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md
# Expected: >= 8

# 4. Dev log exists
test -f docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md && echo OK

# 5. INDEX.md has the runbook row
grep "WALLET_DISCOVERY_V1_RUNBOOK" docs/INDEX.md
# Expected: one matching line

# 6. ROADMAP.md has 4 checked items under Wallet Discovery
grep -A 6 "Wallet Discovery v1" docs/ROADMAP.md | grep -c "\[x\]"
# Expected: 4

# 7. CLI still loads (no accidental code changes)
python -m polytool --help > /dev/null 2>&1 && echo "CLI OK"
```
</verification>

<success_criteria>
- CURRENT_STATE.md says "Shipped" for Wallet Discovery v1, not "pending"
- ROADMAP.md says [SHIPPED] with all 4 items checked
- INDEX.md feature row says "Shipped" and runbook row exists in Workflows table
- Runbook at docs/runbooks/WALLET_DISCOVERY_V1_RUNBOOK.md with all 8 sections
- Dev log at docs/dev_logs/2026-04-10_wallet_discovery_v1_truth_sync_and_release_checklist.md
- Zero grep hits for stale Wallet Discovery "implementation pending" in touched docs
- No code, migration, test, or policy changes made
</success_criteria>

<output>
After completion, create `.planning/quick/260410-ii8-truth-sync-wallet-discovery-v1-docs-and-/260410-ii8-SUMMARY.md`
</output>
